# FILE: configs/controllers/lerobot_diffusion.toml
# LeRobot diffusion policy adapter
# Requires: pip install lerobot
#
# Usage:
#   python -m demos.demo_base_tracking --controller lerobot_diffusion
#   python -m demos.demo_arm_trajectory --controller lerobot_diffusion

type = "lerobot_diffusion"
name = "lerobot_policy"
policy_type = "diffusion"
checkpoint = "huggingface/lerobot-diffusion-lekiwi"
use_camera = false
device = "cpu"