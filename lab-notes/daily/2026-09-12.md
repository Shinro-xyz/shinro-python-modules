# Lab Notes — 2026-09-12

### 2026-09-12 04:00 UTC — initial

### 2026-09-12 ~15:50 UTC — fix: add missing Luenberger twin for inverted pendulum

Commit 9dc99d5 added `src/shinro/configs/estimators/kalman_inverted_pendulum.toml` but not the
required `luenberger_inverted_pendulum.toml` twin, tripping the config-parity drift guard in
tests/test_factories.py (`TestEstimatorConfigParity::test_every_kalman_config_has_luenberger_twin`).
That guard globs `kalman_*.toml` and asserts a matching `luenberger_*.toml` exists with agreeing `dt`
and `observer_gain` length == `process_noise` length.

CI run 34671837300 (main @ 9dc99d5) failed on the `test (3.12)` matrix job at `pytest tests/ -v --tb=short`;
the 3.13/3.14 jobs were cancelled by fail-fast. The failure is deterministic on every Python version
(parent 2508d2c was green — the only test-relevant delta was the new estimator config).

Fix: added `src/shinro/configs/estimators/luenberger_inverted_pendulum.toml` (2 states, gain [0.8, 0.8],
dt plant-derived, mirroring the existing double-pendulum twin style).

Verification: `pytest tests/test_factories.py` — 42 passed. Standalone construction requires a plant
(dt/B_dynamics), identical to the existing `luenberger_cartpole.toml`, so this is a config-parity fix
only, not a behavior change.

### 2026-09-12 15:56 UTC — update

### 2026-09-12 16:30 UTC — update


