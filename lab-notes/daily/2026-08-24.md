# Lab Notes — 2026-08-24

### 2026-08-24 15:30 UTC — initial

### 2026-08-24 — XLA-style tracing layer landed (slice a)

**What was built.** A new `src/shinro/codegen/` subpackage implementing the
tracing model we designed over the last several sessions. This is slice (a)
of the "compile the control loop to a native `.so`" pipeline: tracer +
interpreter + composition, all Python, no Zig yet. The tracer captures a
component's math as a primitive graph by running it once with abstract
`Tracer` values; the interpreter replays the graph on real numpy inputs
(the correctness oracle); the composition pass stitches per-node graphs
into one closed-loop step graph per the fixed ABC dataflow.

**Why.** The deployed `.so` (slice b, future) must be guaranteed to agree
with the Python loop. The tracing model — where the graph *is* the Python
execution, transcribed — makes disagreement structurally impossible: the
only thing that can be wrong is the ~20 primitive lowerings, which are
verified once against `NumpyBackend` and reused for every component. Slice
(a) proves the tracer is sound before any Zig exists.

**Key design decisions.**

- **Pure-registry op dispatch** (no if/elif ladder). `ops.py` is a dict of
  `@register_op` handlers; the interpreter is a 5-line loop over
  `OP_HANDLERS[node.op]`. Adding a new op is one decorator — no interpreter
  edit. This is the extensible "switch" the user asked for.
- **Auto-inferred contracts** (no per-component metadata). `infer_contract.py`
  derives a component's I/O surface from its ABC (`Controller` → `compute`,
  `StateEstimator` → `estimate`) and `inspect.signature`. A new component
  added with `@register_controller("Foo")` + `from_config` + a standard
  `compute(self, current, target)` signature traces with zero tracer-side
  code — the "out of the box" property.
- **Attr-diff state detection.** The tracer snapshots every array-valued
  instance attr's `id()` before the call; any attr whose `id()` changed
  after is flagged as recurrent state (e.g. `KalmanFilter.x_hat`). No
  hand-declared state slot registry.
- **Auto-wiring composition.** The wiring is the fixed ABC dataflow
  (`estimator → controller`), not a per-scenario edge dict. Shape mismatches
  between estimator and controller (KF's `(n,1)` column vectors vs LQR's
  `(n,)` flat) are resolved by auto-inserting `reshape` nodes — the XLA
  approach.
- **`__array_ufunc__ = None` on `Tracer`.** The `ArrayBackend` docstring
  notes that `@` is intentionally not wrapped by the backend, so the tracer
  intercepts `@` at the value level via `__rmatmul__`. Without disabling
  `__array_ufunc__`, numpy would silently coerce the tracer to an array
  and compute with garbage instead of recording a node.

**Bugs found and fixed during the build.**

1. **Stale Tracer leak across traces.** The KF's `self.P` (covariance) gets
   reassigned to a `Tracer` during a traced call (because `K_gain` is a
   Tracer and `(I - K_gain @ C) @ P` propagates Tracer-ness). On the next
   trace, the stale `Tracer` referenced dead node ids from the previous
   graph → `KeyError`. Fix: `trace_node` now snapshots *all* array attrs
   before the call and restores them in a `finally`, so a traced call never
   leaves the component polluted.
2. **2D @ 1D matmul rejected.** `LQR.compute` does `self.K @ error` where
   `K` is `(3,3)` and `error` is `(3,)` — numpy allows this (treats 1D as a
   column vector, returns 1D), but the tracer's shape-check rejected it.
   Fix: `_matmul_out_shape` now handles all three numpy matmul shape
   conventions (2D@2D, 2D@1D, 1D@2D).
3. **Real ndarrays passed to `bk.*` methods.** The KF's covariance update
   `P = A P Aᵀ + Q` involves only real arrays (no Tracer), so `@` computes
   for real and `S` arrives at `bk.inv(S)` as a real ndarray, not a Tracer.
   Fix: `TraceBackend._emit` lifts any concrete ndarrays to `const` nodes
   before emitting — the all-const subgraph gets baked, which is correct
   (it would be folded regardless).
4. **Composition pass placeholder rewiring.** Initial design copied
   `input` nodes from subgraphs into the combined graph, then rewired
   consumers — but the placeholder `input` nodes themselves still got
   interpreted and looked up names that weren't in the combined inputs.
   Fix: `_merge_and_rewire` skips `input` nodes entirely and rewires
   consumers directly to the combined-graph source nodes (with auto-reshape).

**Test results.** 15 new tests, all passing:

- `tests/test_codegen.py` (10): KF trace == numpy (50 inputs, atol=1e-12),
  LQR trace == numpy (50 inputs), graph-structure assertions (KF has
  matmul+inv, LQR is a single matmul, KF detects x_hat state, LQR is
  stateless), tracer primitive unit tests (matmul, `__rmatmul__`, sub, neg,
  transpose).
- `tests/test_codegen_compose.py` (5): trivial-node composition unit test,
  KF+LQR composed step == numpy loop (50 inputs), Luenberger+LQR swap
  (controller graph reused), KF+PID swap (estimator graph reused, PID's
  `clip`/`where`/`copy` ops exercised), Luenberger+PID both-swapped.

Full suite: 229 + 225 existing tests still pass — no regressions. Ruff clean.

**What's next (slice b).** The Zig lowering: `lower_zig.py` walks the
captured graph and emits `base_capsule.zig` (the `shinro_step` C-ABI
function with baked constants), `runtime/linalg.zig` (the shared matvec/inv
helpers), `zig build-lib` to produce `base.so`, and a `ctypes` cross-check
in the test suite asserting the `.so`'s output matches the interpreter to
float-exactness. The tracer is proven sound; the lowering is the next
vertical slice.


### 2026-08-24 16:00 UTC — update



### 2026-08-24 17:00 UTC — update



### 2026-08-24 17:30 UTC — update



### 2026-08-24 18:00 UTC — update

### 2026-08-24 — codegen test coverage expanded

**What was built.** 65 new tests appended to the existing codegen test files
(`tests/test_codegen.py` +57, `tests/test_codegen_compose.py` +8), taking the
codegen suite from 15 to 80 tests. The new tests are direct unit tests for
the lower layers that were previously only exercised indirectly through the
KF/LQR/PID end-to-end traces.

**Why.** The end-to-end tests prove the tracer is sound for the components
that happen to be wired into scenarios, but they don't pin down the
individual primitives, error paths, or restoration guarantees. If a future
component breaks `_lift` or the backend restoration, the end-to-end tests
would catch it late and ambiguously. These unit tests lock the contract of
each layer directly.

**What's covered (by module).**

- `tracing.py`: `_lift` promotion (tracer passthrough, ndarray/list/tuple/
  scalar → const), `_matmul_out_shape` (all three numpy conventions + error
  paths), `_broadcast_shape` (0-d, size-1, rank mismatch, conflicting dims),
  remaining operator overloads (`__add__`/`__radd__`, `__mul__`/`__rmul__`,
  `__rsub__` operand order, 1D transpose rejection).
- `trace_backend.py`: every named method (`array`, `zeros` both call forms,
  `zeros_like`, `eye`, `diag` 1D + 2D-rejection, `copy`, `inv`, `solve` 2D/1D,
  `clip` lo/hi attrs, `where` input order, `any` 0-d, `stack` + empty-list
  rejection, `to_numpy`/`from_numpy`), plus the two loud guards: `allclose`
  rejects traced values and unknown methods raise the actionable
  "register_op" error.
- `ops.py`: `available_ops` sorted/complete, `has_op`, `missing_op_error`
  message format, and a `register_op` round-trip (register a custom op,
  interpret a graph using it, then remove it in `finally` so the global
  registry isn't polluted).
- `interpreter.py`: const→output, `KeyError` on missing input, `NotImplementedError`
  on unknown op, `interpret_step` state/output splitting.
- `infer_contract.py`: LQR/KF contract inference, `TypeError` for non-ABC
  components, `snapshot_instance_attrs`/`detect_state` (scalars excluded,
  reassignment detected via id-change), `_is_array_like`.
- `trace_node.py`: `KeyError` on missing input shape, backend restored after
  trace, array attrs restored to ndarrays (no Tracer leak), `trace_node_with_state`
  returns the restored caller-passed values (not Tracers), `_collect_output_nodes`.
- `compose.py`: `input_limits=None` (no clip node, un-clipped u == numpy),
  `ComposedGraph` port metadata, estimator-with-no-output `ValueError`,
  `_ensure_shape` passthrough/reshape/ShapeMismatchError, `_is_reshape_possible`,
  `_lookup_input_shape`.

**Test results.** 80/80 codegen tests pass; full unit suite 929 passed, 5
skipped, 0 failures. `ruff check` clean on both files. (Note: `ruff format
--check` flags pre-existing multi-line assert style in these files — the repo
lint target only runs `ruff check`, so I left formatting untouched to avoid
noise.)



### 2026-08-24 18:30 UTC — update


### 2026-08-24 — ONNX RL policy adapter landed

**What was built.** A new `OnnxRLAdapter` controller
(`src/shinro/controllers/onnx_rl_adapter.py`, registered as `onnx_rl`) that
wraps any ONNX-exported RL policy as a first-class `Controller`, usable with
the same `--controller` flag as LQR/MPC/PID. Inference runs through
`onnxruntime` with zero torch/gym/framework dependency at deploy time.

**Why.** The suite already had `LeRobotDiffusionAdapter` for HuggingFace
LeRobot policies specifically. The user wanted the broader capability: plug
in policies trained in *any* external RL stack (sb3, RLlib, CleanRL, custom
PyTorch, JAX, TF) via the universal ONNX export step. ONNX was chosen over
torch-checkpoint/jit because it is framework-agnostic at runtime and
deployment-friendly — the exact "inference-only, no training env wrapper"
scope the user confirmed.

**Key design decisions.**

- **Three action-space conventions, one class.** TOML `action_space` field
  dispatches post-processing: `continuous` (raw vector, optionally
  `action_scale`/`action_bias` for tanh-squashed policies),
  `discrete` (logits → one-hot), `stochastic` (`[mean; log_std]` → Gaussian
  sample). `deterministic = true` (default) returns argmax/mean.
- **Sampling default was flipped** to deterministic per the user's earlier
  convention (oracle-testable graphs); `deterministic = true` is the default
  and `false` samples with a config-seeded RNG. Noise lives in Python
  post-processing, **not** in the ONNX graph — consistent with the
  "pass noise as input, don't lower a PRNG to Zig" decision from the codegen
  memory notes. `reset()` re-seeds so episodes are reproducible.
- **Config-driven obs encoder** (`_ObsEncoder`): integer indices into the
  flat plant state vector (user chose indices over named keys since plants
  expose flat arrays), optional `(x - mean)/std` normalization, clipping,
  and `add_batch_dim` for ONNX `[B, obs_dim]` inputs. Pure function —
  feed-dict → batch → float32.
- **Introspection defaults** — `from_config` reads input/output tensor names
  from the session when not given, so minimal config is just
  `model_path` + `action_space`.

**Files changed.**
`src/shinro/controllers/onnx_rl_adapter.py` (new), `src/shinro/configs/
controllers/onnx_rl.toml` (new), `tests/unit/test_onnx_rl_adapter.py` (new,
11 tests), `src/shinro/controllers/__init__.py` (export), `tests/test_factories.py`
(registry set), `pyproject.toml` (new `onnx-rl` optional extra).

**Test results.** 11 new tests pass (obs encoder selection/normalization/
clip, continuous + scale/bias/clip, invalid action_space, discrete argmax +
sampled one-hot, stochastic mean + seeded reproducibility, reset re-seed).
Full suite 940 passed, 5 skipped — no regressions. `ruff check` clean,
pyright 0 errors on the adapter.

**Deliberately deferred** (from memory/plan): recurrent/stateful policies
(needs a state box mirroring the KF `state_x_hat` mechanism — same graph
shape as NN policy + adaptive control), training-side env wrapper (separate
`shinro.envs` module), and ONNX → Zig codegen lowering (relu/tanh/sigmoid
ops + flat op chain, already mapped in the tracing notes).

**What's next.** Recurrent-policy state box if the user hits an RNN/GRU
policy; otherwise the ONNX → Zig lowering slice so ONNX policies can be
compiled into the `.so` path alongside KF+LQR.

### 2026-08-24 — ONNX RL adapter test coverage expanded

**What was built.** 19 new tests in `tests/unit/test_onnx_rl_adapter.py`
(11 → 30) targeting branches the original suite didn't reach. The stub
model builder was parametrized on input/output tensor names so non-default
I/O naming can be exercised.

**Why.** The original 11 tests covered happy paths only. The new tests lock
down the config-parsing surface of `from_config` (the most likely source of
regressions — every branch there was previously untested) and the
`_postprocess` branches that the stub linear model structurally can't
produce (softmax edge cases, log_std clamping, reshape).

**What's covered (by area).**

- **`from_config` surface** (`TestFromConfigSurface`, 9): default
  `action_space`/`state_keys` (inferred from input shape), custom
  input/output tensor names, `observation.input_name` override,
  `observation.normalize` and `observation.clip` wiring, one-sided
  `action_clip_low`-only and `action_clip_high`-only, and `KeyError` when
  `normalize=true` without `obs_mean`/`obs_std`.
- **`_postprocess` direct** (`TestPostprocessDirect`, 9): discrete logits
  reshape-flat, softmax stability with extreme logits (max subtraction),
  tie-break picks first argmax, log_std clamp to `[-10, 2]` (upper bound
  keeps samples at O(20) rather than `exp(100)`, lower bound collapses
  samples onto the mean), deterministic mean passthrough with scale/bias +
  float32 dtype, continuous float64→float32 cast.
- **`compute` robustness**: batched raw output flattened back to `(n_u,)`,
  plain-Python-list state input, and `target` argument ignored (same output).

**Test results.** 30/30 adapter tests pass; full suite 959 passed, 5
skipped, 0 failures — no regressions. `ruff check` clean.

### 2026-08-24 — ONNX RL adapter made backend-agnostic

**What was built.** `OnnxRLAdapter` was refactored from numpy-only to the
framework's `ArrayBackend` abstraction: it now accepts backend-native state
(numpy **or** torch) and returns actions in the same backend's native type.
The ONNX boundary itself still requires numpy (onnxruntime's feed contract),
so conversion happens in two narrow, explicit places: `_ObsEncoder.encode()`
calls `bk.to_numpy(state)` before indexing/normalizing, and
`compute()` returns `bk.from_numpy(action)` after post-processing.

**Why.** Every other component in the suite (LQR, PID, KF, plants,
trajectories) takes an optional `backend` and `from_config(config,
backend=...)`. The adapter was the outlier, hard-wired to numpy — which was
not just inconsistent but a latent bug: `ControllerFactory.create()` passes
`backend=` positionally through `from_config`, so building an `onnx_rl`
controller through the factory would have crashed on the unexpected kwarg.

**Key design decisions.**

- **Boundary-only conversion, not full port.** The `_postprocess` logic
  (logits → softmax, `[mean; log_std]` → sample, tanh-squash scaling) stays
  in numpy. It operates on the raw ONNX output inside the boundary, and both
  numpy and torch share the same exact result — the framework's other
  components already accept this pattern (e.g. `lerobot_adapter` runs the
  policy in torch and converts at its boundary). Porting post-processing to
  `bk.*` would also be trace-hostile for the future Zig lowering.
- **Encoder gets its own `bk`.** `_ObsEncoder` stores the backend too, since
  it is the other side of the numpy boundary (state → feed dict). Building
  it happens in `from_config`, which now accepts and threads `backend`.
- **Signed signature matches the rest of the suite.** `from_config(cls,
  config, backend=None)` — identical to LQR/PID/KF — so the factory
  passthrough works out of the box.

**Test results.** 4 new tests (`TestBackendAgnostic`): torch backend returns
a `torch.Tensor` from `compute` (values verified via `torch.testing.assert_close`),
torch input + normalized observations, torch discrete action space, and the
`ControllerFactory` backend-passthrough path (which previously would have
crashed). Full suite 963 passed, 5 skipped, 0 failures. `ruff check` clean,
pyright 0 errors on the adapter.



### 2026-08-24 20:00 UTC — update



### 2026-08-24 20:30 UTC — update



### 2026-08-24 21:30 UTC — update


