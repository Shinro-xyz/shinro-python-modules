# 2026-09-07

## Graph-conditional OSQP linking for the Zig VM

### What changed

The Zig lowering path now decides whether an OSQP solver belongs in the binary
based on the lowered graph itself, rather than always compiling the generated
static solver into `libbase.so`.

- `lower_zig` now emits `pub const has_solve_qp = true/false` into
  `graph_data.zig` and records the same fact in the graph manifest.
- `build.zig` reads that flag. For QP graphs it wires in the selected
  `-Dsolver_dir`, its headers, C sources, and `solver_meta`. For non-QP graphs
  it omits those entirely and leaves the build manifest's solver provenance as
  `null`.
- `lower.zig` imports `qp.zig` and `solver_meta` only when the graph has a
  `.solve_qp` node, so the OSQP wrapper is not analyzed for LQR/PID builds.
- Updated the Zig lowering tests and docs to describe the solver-free path and
  the conditional manifest fields.
- Excluded the generated `runtime/codegen/emosqp/setup.py` from Ruff because it
  is generated OSQP output, not source we format.

### Why

The default graph is KF + LQR and has no `.solve_qp` node, yet the build was
still linking the whole OSQP codegen bake. This made non-MPC binaries carry
solver code they can never call. The graph is the authoritative source of truth
for whether the deployed step performs QP, so making the graph carry that
decision lets Zig build one self-consistent binary per controller scenario.

### Key design decision

Keep the decision in generated graph data, not a separate build flag. This
preserves the existing contract that one graph is one deployment artifact and
avoids asking the caller to remember whether the graph contains a QP. The
`has_solve_qp` flag is cheap and deterministic, and the existing comptime
graph↔bake `n_vars` check still applies when QP is present.

### Test results

- `ZIG_GLOBAL_CACHE_DIR=/tmp/shinro-zig-cache make test-zig` — 15 passed.
- `make zig-build` — passed with the shipped KF + LQR graph.
- `ruff check .` — passed after excluding the generated OSQP setup file.
- Local ReleaseFast size check: KF + LQR `libbase.so` was about 201 KB versus
  267 KB for KF + MPC_LTI (about 66 KB smaller with OSQP omitted). The LQR
  manifest reported `solver: null` and `nm -D` showed no OSQP/QDLDL symbols.

### 2026-09-07 15:30 UTC — update

### Deployment master hash — config/graph/binary tracking

### What changed

Added a post-compile **deployment record** that gives every build a single
master hash committing to the whole config → graph → solver → binary chain,
so teams can answer "which estimator/controller pair is deployed, and has
anything drifted?" without rebuilding.

- `scripts/gen_emosqp_test.py` — `solver_meta.zig` now records
  `config_sha256` (sha256 of the resolved config's raw bytes) plus the
  python/numpy/scipy/osqp versions. The generic `solverMetaJson()` parser in
  `build.zig` surfaces them in the build manifest with zero build changes.
- `src/shinro/codegen/lower_zig.py` — `lower_zig()` gained an optional
  `provenance` dict, merged into the graph manifest. Stays generic/opaque.
- `scripts/gen_base.py` / `scripts/gen_mpc.py` — pass provenance: the config
  paths + sha256 they consumed, plus python/numpy versions. `gen_mpc.main()`
  derives the controller config from its actual argument.
- `scripts/stamp_deployment.py` (new) — post-compile step wired into
  `make zig-build`. Reads the build manifest, hashes `libbase.so` and the
  baked solver tree, writes `libbase.deployment.json` (master hash + per-slot
  drill-down) and a timestamped archive copy under `deployments/`.
- `scripts/verify_deployment.py` (new) — independent re-hash of the artifacts
  against the record (producer/verifier separation); exit 0 on match, 1 on
  drift. Catches a config edited after the bake with no rebuild.
- `.gitattributes` — `*.toml text eol=lf` pins TOML checkouts to LF so the
  byte-level config hashes are reproducible across platforms (Windows
  `core.autocrlf` would otherwise flip them).
- `tests/test_zig_lowering.py` — new `TestDeploymentRecord` class (5 tests):
  master-hash/slot shape, determinism, archive copy, verify pass + drift
  detection, and graph-provenance plumbing. `_build_so` gained a
  `provenance` param.

### Why

The build manifest tracked the *build* (graph sha, solver facts) but never
hashed the `.so` itself, and configs were recorded only as path strings — so
config drift was invisible and a deployed binary couldn't be verified without
rebuilding. The master hash closes both: `binary_sha256` is the ground truth
of what's deployed, `config_sha256` makes drift detectable read-only.

### Key design decisions

- **Master hash computed in Python post-compile, not `build.zig`** — the
  manifest is written at configure time, before the `.so` exists, so the
  binary slot can't be computed there. A separate record file
  (`libbase.deployment.json`) keeps `build.zig`'s manifest a pure
  configure-time artifact.
- **Fixed 4-slot structure with a sentinel** (`sha256(b"")`) for the solver
  slot — LQR/PID and MPC deployments stay structurally comparable; switching
  controllers changes slot *values*, never the shape.
- **Flat hash for the solver slot** (not a Merkle tree) — 40 files, flat is
  90% of the value; the tree only earns its keep for membership proofs.
- **Config hash is hermetic** — hashes the resolved file's raw bytes;
  `config_resolver.py` is path-only (no preprocessing), so the hash covers
  exactly what the factories consumed.
- **Source code deliberately excluded** — the master hash means "configs +
  generated + binary", not "the whole software". Editing a controller `.py`
  without regenerating is invisible (documented residual risk; process
  discipline mandates regeneration).

### Test results

- `make test-zig` — 20 passed (15 existing + 5 new deployment-record tests).
- `make lint` — ruff + pyright clean.
- Manual cross-check: `stamp_deployment.py` binary slot == `sha256(libbase.so)`;
  `verify_deployment.py` returns 0 on match, 1 after tampering a config byte.
- Determinism: re-stamping the same build produces a byte-identical record
  (archive filename differs by timestamp only).



### 2026-09-07 17:00 UTC — update

### 2026-09-07 18:30 UTC — standalone component tracer (`scripts/trace_component.py`)

Phase 2 of the unified-CLI issue (#19): a standalone "does my component
trace?" gate, no build machinery. Three modes in one script:

- **Inventory** (no args) — every registered controller/estimator/trajectory/
  plant, cross-referenced with the config TOMLs that exist for them (matched
  on each file's `type` field).
- **Contract** (`--list <config>`) — the inferred trace contract
  (`Type.method(input_names)`), so users discover what shapes to pass.
- **Trace + oracle** (`<config> --shape ... [--state ...]`) — `trace_node`,
  node count + op histogram + `has_op` coverage, then the oracle check:
  re-execute the captured graph with `interpret()` on the same random inputs
  as a live component call, asserting **bit-exact** agreement per output and
  `state_*` port. Exit codes: 0 green · 1 untraceable · 2 usage/config · 3
  oracle mismatch.

Design decisions:

- **Shapes are explicit flags, not guessed.** Configs don't declare dims
  (`lqr_base.toml` is just costs), so `--shape name=SPEC` / `--state name=SPEC`
  (SPEC = `3`, `3x1`, `3x3`, `scalar`). `--list` makes discovery cheap.
- **Square 2-D state inputs are SPD-ified** (`A@A.T + 0.1·I`) so covariance
  states (KF's `P`) stay invertible for the oracle — mirrors the test-suite
  convention.
- **Any trace failure is a clean "not traceable" report** (exit 1), not a
  traceback — the script's contract is to answer the question, including
  "no".

Findings (the script earning its keep):

- **Fixed a real bug in `PID.from_config`**: gains were passed through
  `bk.from_numpy(...)`, which is a no-op for `NumpyBackend`, leaving `self.ki`
  etc. as Python lists. Works under numpy (list·array broadcasting) but
  crashes the `TraceBackend` (`zeros_like(list)` → AttributeError), so PID
  was untraceable via config. Switched to `bk.array(...)` (matching LQR).
  No test depended on list gains; full suite green.
- **SMC is not traceable as-is** — `self.bk.array(x).flatten()` calls
  `.flatten()` on a `Tracer` (no such method). Would need a `flatten` op.
- **Plants with state indexing are not traceable as-is** —
  `self.state[2]` on a pre-injected state `Tracer` (no `__getitem__`).
- **Trajectory `from_config` returns schedule data, not a component** — the
  script detects this and reports "not standalone-traceable" (exit 2).

Test results: `tests/test_trace_component.py` — 13 passed (LQR, KF w/ SPD
state, PID w/ 3 state ports, SMC untraceable, trajectory non-component,
exit codes, CLI subprocess modes). `make lint` clean; `make test` 968 passed,
5 skipped, 73 deselected.



### 2026-09-07 20:30 UTC — update

### E2E scenario compilation pipeline (gen_scenario → build_scenario)

### What changed

Closed the gap between "trace a component" and "deploy a `.so`": an external
user with a different robot can now compile their estimator + controller into
a verified, stamped `libbase.so` from a single scenario TOML — no bespoke
generator scripts.

- `src/shinro/codegen/build.py` (new) — generic `build_composed_graph(
  estimator_config, controller_config, n_x, n_u, input_limits)` with a
  **two-pass trace** (`_trace_with_state`): pass 1 attr-diffs to discover
  recurrent state, pass 2 re-traces with those attrs pre-injected as `state_*`
  ports. This is what makes component swaps (KF→Luenberger, LQR→PID) compose
  with zero per-component declaration. Controller inputs map to shapes by role
  (`u_prev` → `(n_u,)`, else `(n_x,)`); estimator inputs follow the repo's
  `(n,1)` column convention.
- `scripts/gen_base.py` / `scripts/gen_mpc.py` — refactored onto
  `build_composed_graph` (thin wrappers now; provenance preserved).
- `scripts/gen_scenario.py` (new) — zig-free stage: reads a scenario TOML's
  `[controller]`/`[estimator]` configs, `[scenario].input_limits`, and the new
  `[compile]` section (`n_x`, `n_u`), traces + composes + lowers to an
  **isolated** path (never clobbers `runtime/graph_data.zig`). Records the
  scenario TOML's own sha256 in provenance, so the deployment record commits
  to the whole build spec.
- `scripts/build_scenario.py` (new) — zig stage: pre-flight zig check (exit
  5), build flags from `[compile]` (CLI > TOML > default), `zig build
  -Dgraph=<abs>`, **integrity check** (re-gen + byte-compare manifests —
  deterministic lowering proves the `.so` was built from exactly this
  scenario's graph), **oracle B** (ctypes `shinro_step` vs `interpret()` on N
  random inputs, tol 1e-12 / 1e-3 for QP), then `stamp_deployment` +
  `verify_deployment`.
- `Makefile` — `make compile SCENARIO=...` chains the two scripts.
- `tests/test_compile_scenario.py` (new) — 8 tests: `[compile]` validation,
  gen-vs-shipped graph equivalence, e2e verified build, stale-graph rejection,
  shared-path-untouched.
- `tests/integration/scenarios/base_tracking.toml` — added `[compile]`
  (`n_x = 3`, `n_u = 3`).

### Why

The compile-to-`.so` path was "folklore": `make zig-build` only built the
shipped KF+LQR graph, and a different robot meant hand-copying `gen_base.py`
and editing it. The framework pieces (`trace_node`, `compose`, `lower_zig`,
the Zig VM) were already generic — only the generator scripts hardcoded
LeKiwi. This makes the scenario TOML the single reproducible build spec: one
config drives both simulation (`ScenarioFactory`) and compilation.

### Key design decisions

- **`[compile]` section is the build spec** — `n_x`/`n_u` (baked at trace
  time), `optimize` (`debug`/`release` → ReleaseFast only; ReleaseSafe hangs
  in `osqp_solve`, ReleaseSmall unvalidated), `target` (cross-compile),
  `solver_dir` (required for QP graphs). Unknown keys and invalid `optimize`
  are loud errors (exit 2) — a typo must not silently change the build.
- **Two scripts, unified by make** — `gen_scenario.py` (zig-free, testable in
  CI without zig) and `build_scenario.py` (zig + oracle + stamp + verify).
  The handoff contract is `graph_data.zig` + its manifest; provenance flows
  through the existing chain (graph manifest → build manifest → deployment
  record) with zero new coupling.
- **`--scenario` makes "verified" possible** — the oracle needs the Python
  `ComposedGraph` (not the Zig table), so `build_scenario.py` re-runs the gen
  stage in-process and byte-compares manifests (integrity) before trusting the
  oracle. Omitting `--scenario` is allowed-with-warning (build-only,
  unverified).
- **Exit codes** — 0 ok · 1 untraceable · 2 usage/config · 3 oracle mismatch
  · 4 build/verify failure · 5 zig missing (pre-flight, before any work).

### Test results

- `make test` — 978 passed, 5 skipped (8 new e2e tests).
- `make test-zig` — 20 passed (refactor fidelity: all KF+LQR/PID/MPC oracles
  green against the refactored `build_base_graph`).
- `make compile` — end-to-end green on `base_tracking.toml`; oracle max abs
  err 4.996e-16; `--optimize release` → ReleaseFast + stripped, ~200 KB.
- `make lint` — ruff clean.
- Failure paths verified: zig missing → 5, corrupt manifest → 2, stale graph
  → 2, QP graph without solver → 2.
- `runtime/graph_data.zig` untouched after custom compiles (isolated
  `-Dgraph`).

### 2026-09-07 22:00 UTC — update

### Generic scenario config template

### What changed

Added `src/shinro/configs/scenarios/_template.toml` — a commented scenario
skeleton for external users with a different robot. Copy it, fill in the
placeholders (controller config, estimator config, `[compile]` `n_x`/`n_u`),
and run `make compile`. The template documents every field the schema
supports: required vs optional sections, valid `optimize` values, the
`target`/`solver_dir` options, and which sections the simulation path needs
vs. the compile path (the sim block is commented out with a "delete for
compile-only robots" note).

Also added a drift-guard test (`test_scenario_template_stays_in_sync_with_compile_schema`):
the template must parse as TOML, use only known `[compile]` keys, and mention
every schema key (active or commented) — so a future `[compile]` schema
addition forces the template to document it.

### Why

The e2e pipeline works, but a new user had no starting point: the only
scenario TOMLs were LeKiwi simulation scenarios in `tests/integration/`
(carrying `[sim]`/`[physics]`/`[plant]` sections that don't resolve from an
installed wheel). The template makes the minimal compile-only scenario shape
explicit and copy-pasteable.

### Key design decisions

- **Config-as-documentation** — the template is the schema documentation, in
  the same format the pipeline consumes. No script changes; `load_scenario`
  already accepts the minimal form.
- **`_` prefix** signals "not runnable"; the `trace_component.py` inventory
  ignores it (scenario files have no top-level `type`).
- **Placeholders are valid TOML** — `n_x = 2  # <fill in>` rather than
  `n_x = <2>`, so the file parses and the drift-guard test can read it.

### Test results

- `tests/test_compile_scenario.py` — 9 passed (8 existing + 1 drift guard).
- Manual: filled the template with the shipped LQR/KF configs and `n_x=n_u=3`
  → `gen_scenario.py` produces the same 50-node graph as `base_tracking.toml`;
  wrong dims (2 vs 3-DOF configs) → clean exit 2 (ShapeMismatchError is a
  ValueError, caught as usage error).
- `make lint` — ruff clean.



### 2026-09-07 21:30 UTC — update



### 2026-09-07 21:47 UTC — update



### 2026-09-07 23:45 UTC — update

### `shinro-testing` skill + testing docs cleanup

### What changed

Promoted the testing workflow into a repo-local OpenCode skill, mirroring the
existing `port-numpy-component` skill pattern, and cleaned up the stale
`docs/testing.md`.

- `.opencode/skills/shinro-testing/SKILL.md` (new) — the agent-facing decision
  procedure for running and debugging the suite: command cheat-sheet (verified
  against the Makefile), the six silent-skip gotchas (marker exclusion,
  per-file targets vs the slow horizon test, zig-gated whole-file skips,
  optional-extra `importorskip`, `bk` ×2 parametrization, generated
  `graph_data.zig` last-build-wins), result interpretation, and MUST DO /
  MUST NOT DO constraints.
- `docs/testing.md` — fixed stale facts: `addopts` now shows both markers
  (`-m 'not integration and not mcp'`), `pythonpath` includes `"."`, and the
  CI section (which referenced `.github/workflows/ci.yml` / `test.yml` that
  **do not exist** — the `.github/` tree is absent) was replaced with an
  accurate "no CI config in-repo" note. Added a pointer to the new skill.
- `run_tests.py` — fixed `--quick`: it ran only `tests/test_mcp_server.py`
  (a bug), now runs the same unit-file list as `make test-quick`.
- `AGENTS.md` — added a "Running and Debugging Tests" section pointing at the
  skill, alongside the existing `port-numpy-component` pointer.

### Why

The testing knowledge lived in three places that had drifted apart: `AGENTS.md`
(brief), `docs/testing.md` (stale — wrong `addopts`, phantom CI workflows), and
the Makefile (ground truth). The user asked to "make the testing part a skill
in the repository" so the operational procedure is loadable on-demand by an
agent, with the docs fixed to stop contradicting the TOML/Makefile.

### Key design decisions

- **Skill = decision procedure, docs = reference.** The skill encodes *which
  suite to run for a given change* and *how to tell a real pass from a silent
  skip*; `docs/testing.md` stays the human reference. Both cross-link.
- **Trust `pyproject.toml` / `Makefile` over docs** — encoded as a MUST DO in
  the skill, since docs drift.
- **`run_tests.py --quick` aligned to `make test-quick`** — the wrapper is a
  thin pytest forwarder; diverging file lists were a bug, not a feature.

### Test results

- `make test-quick` — green (see verification).
- No source code changed (docs + skill + wrapper file list only); the
  `run_tests.py` change is a test-invocation fix, not a library change.




### 2026-09-07 23:30 UTC — update

### Luenberger estimator parity (configs + tests + `.so` oracle)

### What changed

Completed estimator parity across the framework: every shipped KalmanFilter
config now has a matching LuenbergerObserver twin, and the Luenberger path is
exercised at every level (live integration, config drift guard, compiled `.so`).

- `configs/estimators/luenberger_arm.toml` (new) — 6-DOF fixed gain `[0.8]*6`,
  dt=0.02, mirroring `kalman_arm.toml`. With the sim-backed arm's default
  `A = I` model, the error dynamics `(A − LC) = 0.2·I` are Schur.
- `tests/integration/test_arm_cartesian.py` — new `TestArmEstimatorVariants`
  (KF + Luenberger) mirroring the pendulum/cartpole variant pattern; the arm
  was the last integration scenario with KF-only coverage.
- `tests/test_factories.py` — new `TestEstimatorConfigParity` drift guard:
  every `kalman_*.toml` must have a `luenberger_*.toml` twin with matching
  `dt` and state dimension. A future KF-only config fails loudly instead of
  silently leaving a plant's Luenberger variant untested.
- `tests/test_zig_lowering.py` — new `TestLuenbergerComposedOracle` +
  `luenberger_composed_so` fixture: builds a Luenberger + LQR `.so` via the
  generic `build_composed_graph` (two-pass trace discovers `x_hat`, no `P`),
  and asserts `shinro_step` matches a live numpy closed loop over 100 ticks
  (tol 1e-10). This closes the earlier gap where Luenberger was only verified
  at the graph/interpreter level, never compiled to a binary.
- `docs/components.md` — estimator catalog rows updated to the full four-config
  lists for both estimators.

### Why

The estimator configs were asymmetric: base/pendulum/cartpole had Luenberger
twins (base from before, pendulum/cartpole from the plant-only scenarios), but
the arm had none, and the arm integration test was KF-only. The user asked to
"add all the KF configs to luenberger as well" — this makes the two estimators
drop-in interchangeable everywhere, and adds the missing `.so` oracle so the
Luenberger compile path is proven, not just assumed.

### Key design decisions

- **Fixed gain `[0.8]*6` for the arm** — matches `luenberger_base.toml`'s
  recipe; with the default `A = I` model the error dynamics are Schur. No
  model derivation (arm is sim-backed), so no tuning beyond the gain.
- **Parity guard is one-directional** (KF → Luenberger) — a Luenberger config
  without a KF twin is allowed (e.g. a future observer-only use case), but a
  KF config without a Luenberger twin fails.
- **Oracle uses the generic builder** (`build_composed_graph`), not hand-traced
  nodes — exercises the real two-pass state discovery on the Luenberger path.

### Test results

- `TestLuenbergerComposedOracle` — passed (zig 0.16.0); composed graph is 28
  nodes, solver-free, ports `y/x_ref/u_prev/state_x_hat → u`.
- Arm Luenberger variant sanity run: steady-state tail 0.0238, estimator tail
  0.0149 (tolerances 0.05 / 0.03) — well within.
- `make test-integration`, `make test`, `make lint` — see verification below.




### 2026-09-07 23:00 UTC — update

### Plant-only integration scenarios (pendulum + cartpole)

### What changed

Extended the integration suite beyond the two MuJoCo-wired plants (holonomic
base, arm) to the **analytical plants** — inverted pendulum and cartpole — via
a new plant-only scenario mode that needs no MuJoCo sim.

- `src/shinro/factories/scenario_factory.py` — `[sim]` is now optional. The
  no-sim path builds the plant directly from `_PLANT_REGISTRY` via
  `[plant].type` + `[plant].config`, applies optional `[plant].initial_state`,
  and enforces `[scenario].dt == plant.dt` (the plant self-integrates at its
  own time step). `Scenario.sim` is now `RobotSim | None`.
- **Plant-derived models**: in plant-only mode, a controller/estimator config
  that omits `A_dynamics`/`B_dynamics` gets its discrete-time model derived
  from the plant at build time — `linearize_plant` (upright equilibrium) +
  first-order Euler discretization. Mirrors the MPPI `attach_plant` precedent;
  the plant TOML is the single source of physics truth. Gated to plant-only
  mode so base/arm scenarios keep their `A=I, B=dt·I` defaults (correct for the
  velocity-commanded base).
- `src/shinro/utils/linearization.py` — new `discretize_euler(A_c, B_c, dt)`
  helper (backend-agnostic; Euler chosen over `scipy.expm` to keep the KF
  torch-capable and match the plants' semi-implicit integration).
- `ControllerFactory`/`EstimatorFactory` — accept a `config` dict directly
  (path XOR dict, loud error otherwise) so injected configs reach `from_config`
  without bypassing the factory layer.
- `tests/integration/helpers/scenario_runner.py` — `run_scenario` guards
  `scenario.sim.step()` and the `sim.engine.dt` fallback; `run_phase_schedule`
  raises if `sim is None`.
- **Fixed two latent bugs** in `KalmanFilter.from_config` and
  `LuenbergerObserver.from_config`: the default `D` was `zeros((n_y, n_x))`
  instead of `zeros((n_y, n_u))`. Only worked before because base/arm have
  n_y == n_u == 3; pendulum (n_y=2, n_u=1) and cartpole (n_y=4, n_u=1) exposed
  it as a matmul shape error.
- **Fixed the shipped plant TOMLs**: `inverted_pendulum.toml` and
  `cartpole.toml` used a stale `[key] value = ...` wrapper that
  `from_config` couldn't consume (double_pendulum/holonomic_base already used
  the flat format). Converted to flat consumable format.
- New configs: `lqr_pendulum.toml`, `lqr_cartpole.toml` (Q/R only),
  `kalman_pendulum.toml`, `kalman_cartpole.toml`, `luenberger_pendulum.toml`,
  `luenberger_cartpole.toml`, `pendulum_upright.toml`, `cartpole_upright.toml`.
- New scenarios: `tests/integration/scenarios/pendulum_balance.toml`,
  `cartpole_balance.toml` (no `[sim]`, dt=0.01, perturbed initial state).
- New tests: `test_pendulum.py`, `test_cartpole.py` (regulate-from-perturbation,
  estimator recovery, finite state, estimator-variant matrix), plant-only
  validation cases in `test_scenario_factory.py`, `discretize_euler` unit tests,
  factory config-dict tests, and KF/Luenberger D-shape regression tests.

### Why

The integration suite only exercised the two MuJoCo-wired plants. Pendulum and
cartpole have standalone analytical dynamics and needed no physics engine, but
`ScenarioFactory` hardcoded `RobotSim`. This unlocks them as first-class
scenarios and lands the exact LQR/KF configs + plant-derivation helper that the
planned full-loop `.so` compilation (plant+KF+LQR in one binary) reuses.

### Key design decisions

- **Derive A/B from the plant, don't bake matrices into configs** — the user
  explicitly preferred this ("define in the plant toml and be done"). Controller
  configs carry only Q/R; estimator configs only noise. Changing a mass in the
  plant TOML automatically re-derives the controller/estimator models.
- **Euler discretization over `scipy.expm`** — backend-agnostic (KF stays
  torch-capable) and consistent with the plants' semi-implicit Euler. At
  dt=0.01 the error is negligible.
- **Injection gated to plant-only mode** — base/arm sim scenarios keep their
  `A=I, B=dt·I` defaults byte-compatible; migrating them is a separate cleanup.
- **No `[compile]` sections on the new scenarios** — keeps this change
  independent of the full-loop `.so` work.

### Test results

- `make test-integration` — 56 passed (22 new: 5 pendulum + 5 cartpole + 2
  composes-loop + 4 plant-only validation + 2 variant matrices + 4 unit).
- `make test` — 991 passed, 5 skipped, 94 deselected (no regressions).
- `make lint` — ruff + pyright clean.
- Tuning: pendulum tail steady-state error 0.010 (torque ≤ 0.94 Nm, within ±2);
  cartpole 0.021 (force saturates only on the first 2 steps of the initial
  kick, within ±10 N). Tolerances set to 0.05 steady-state / 0.03 estimator,
  ~2-5× observed.




### 2026-09-07 22:30 UTC — update



### 2026-09-07 23:00 UTC — update



### 2026-09-08 00:30 UTC — update

### Packaging fix: `shinro-compile` ships the full e2e pipeline in the wheel

### What changed

Closed the packaging gap that kept the compile-to-`.so` path repo-only. An
external team can now `pip install shinro` and run `shinro-compile
<scenario.toml>` to get a verified, stamped `libbase.so` — no vendoring the
repo, no `make compile`.

- **Compile pipeline moved into the package** — new modules under
  `src/shinro/codegen/`: `scenario_gen.py` (from `scripts/gen_scenario.py`),
  `scenario_build.py` (from `scripts/build_scenario.py`), `stamp.py` (from
  `scripts/stamp_deployment.py`), `verify.py` (from
  `scripts/verify_deployment.py`), `cli.py` (the `shinro-compile` entry
  point), and `runtime_paths.py` (locates the packaged Zig runtime via
  `importlib.resources`, mirroring `config_resolver`).
- **Zig runtime moved into the package** — `runtime/` → `src/shinro/runtime/`
  (handwritten VM + the shipped default bake + generated graph). The wheel now
  bundles it (109 files under `shinro/runtime/`).
- **`scripts/` became thin shims** — `gen_scenario.py`, `build_scenario.py`,
  `stamp_deployment.py`, `verify_deployment.py` now re-export from the
  package modules, so the existing `from scripts.* import ...` test imports
  keep working. `gen_base.py` / `gen_mpc.py` / `gen_emosqp_test.py` stay
  repo-internal (LeKiwi-specific) but their write paths point at
  `src/shinro/runtime/`.
- **`pyproject.toml`** — `shinro-compile = "shinro.codegen.cli:main"` added
  to `[project.scripts]`; `"runtime/**/*"` added to package-data; ruff exclude
  updated for the moved `emosqp/setup.py`.
- **References updated** — Makefile (`--build-file src/shinro/runtime/build.zig`),
  `tests/test_zig_lowering.py` + `tests/test_compile_scenario.py` (RUNTIME
  const), docs, AGENTS.md, the `shinro-testing` skill, and the moved
  `src/shinro/runtime/README.md`.

### Why

The `_template.toml` told external users to "run `make compile`" — but the
compile toolchain (`runtime/`, `scripts/`, Makefile) was never in the wheel, so
a `pip install` couldn't. The intent (external teams) and the reality
(repo-only toolchain) didn't match. This makes the scenario TOML the single
reproducible build spec for *anyone*, installed or checked out.

### Key design decisions

- **Runtime moved into the package (single source of truth), not duplicated.**
  A packaged copy would drift; moving `runtime/` under `src/shinro/` means the
  wheel ships exactly what the checkout builds. The churn (Makefile, tests,
  docs) was mechanical.
- **`scripts/` kept as shims, not deleted.** The test suite imports
  `from scripts.gen_base import build_base_graph` etc.; shims preserve that
  while the real logic lives in the package. `gen_base`/`gen_mpc` stay
  repo-internal because they're LeKiwi-specific (one fixed graph), not the
  external-team path.
- **`runtime_root()` via `importlib.resources`** — resolves to
  `src/shinro/runtime/` in a checkout and the installed package's `runtime/`
  in a wheel, the same packaged-vs-CWD pattern `config_resolver` already uses.
- **`shinro-compile` chains the two stages** (gen → build+oracle+stamp+verify)
  with the same exit codes as `make compile` (0 ok · 1 untraceable · 2 usage ·
  3 oracle · 4 build · 5 no-zig).

### Test results

- `make test-quick` — 565 passed, 5 skipped.
- `make test` — 993 passed, 5 skipped, 96 deselected.
- `make lint` — ruff + pyright clean.
- `make test-zig` — 21 passed (the moved runtime builds + oracles green).
- `make compile SCENARIO=base_tracking.toml` — green through the shims.
- `python3 -m shinro.codegen.cli <scenario> --out <dir>` — green, oracle max
  abs err 4.996e-16, deployment record stamped + verified.
- **External-team simulation**: built the wheel, installed it in a clean venv,
  ran `shinro-compile` from the venv against a scenario — green end-to-end
  (oracle 4.996e-16, exit 0). The wheel bundles 109 files under
  `shinro/runtime/` including `build.zig`, `lower.zig`, `linalg.zig`,
  `qp.zig`, the shipped `graph_data.zig`, and the `codegen/emosqp/` bake.
- `src/shinro/runtime/graph_data_manifest.json` regenerated by `make zig-gen`
  (the shipped graph's manifest now carries the `provenance` block — the
  correct deterministic output of `gen_base.py`).



### 2026-09-08 00:47 UTC — update



### 2026-09-08 00:49 UTC — update


