# Lab Notes — 2026-08-31

### 2026-08-31 16:42 UTC — initial

### 2026-08-31 — ticket workflow: filled empty lab note, closed issue #11

**What was built.** First session running the new "Check tickets" workflow
(step 0 in AGENTS.md). Picked up issue #11 (P1): `lab-notes/daily/2026-08-27.md`
was an empty header — the docs-drift session never wrote its semantic summary.

**What was done.**

- Reconstructed the 2026-08-27 session from commit `0eb468f` and the issue
  body, and wrote the missing semantic summary: MCP packaging to the
  `shinro-mcp` console script, new `docs/components.md` catalog, codegen docs
  updated to the shipped Zig lowering, new `docs/quickstart.md` +
  `runtime/README.md`, README repo-structure map, package docstring.
- Added the "0. Check tickets" step to the AGENTS.md Workflow section (the
  ritual proposed when the issue tracker was set up) so open issues greet
  every session.
- Closed issue #11 via `gh issue close` with a comment summarizing the fix.

**Key design decision.** The GitHub MCP server is unauthenticated (no token in
`~/.config/opencode/opencode.json`), so issue operations go through the `gh`
CLI instead. Worth wiring up a PAT if we want MCP-native issue tooling later.

**Verification.** `git show 0eb468f` cross-checked the note's claims against
the actual diff; issue #11 confirmed closed.

### 2026-08-31 16:43 UTC — update


