{
  "schemaVersion": 1,
  "memories": [
    {
      "id": "labnote_b7ac1559f1f2194f6efe322d",
      "content": "# Lab note 2026-06-10.md\nSource: lab-notes/daily/2026-06-10.md\n\n---\ntitle: \"2026-06-10 — Initial Commit\"\ndate: 2026-06-10\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-10 — Initial Commit\n**Commit:** `d0d773b`\n\nJust a `.gitignore`. Empty project scaffold. Nothing to see here.",
      "source": "lab-notes/daily/2026-06-10.md"
    },
    {
      "id": "labnote_4516fdafe0240f357eb29d19",
      "content": "# Lab note 2026-06-14.md\nSource: lab-notes/daily/2026-06-14.md\n\n---\ntitle: \"2026-06-14 — Base Kinematics + MuJoCo Simulation\"\ndate: 2026-06-14\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - kinematics\n  - mujoco\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-14 — Base Kinematics + MuJoCo Simulation\n**Last commit:** `b7a13d3` — \"feat: implement inverse kinematics matrix for lekiwi robot\"\n\n**Files:**\n- `lekiwi-robot-base.py` — standalone function `MobileRobotBaseKinematics()` computing the holonomic kinematics matrix and its pseudoinverse\n- `lekiwi-sim/` — full MuJoCo simulation directory with `mjcf_lcmm_robot.xml`, `so_arm100.xml`, 40+ STL mesh files, and a README\n\n**Decisions:**\n- Kinematics was a **standalone function**, not a class. No OOP, no plant abstraction.\n- MuJoCo model imported from Fusion 360 via ACDC4Robot plugin. All meshes simplified.\n- 3 omni-wheel holonomic base (120° spacing) + SO-ARM100 6-DOF arm + gripper.\n- Arm joints: Rotation, Pitch, Elbow, Wrist_Pitch, Wrist_Roll, Jaw — all position-controlled.\n\n**Working:** MuJoCo model loads and renders. Base kinematics function computes the wheel-to-velocity matrix.\n**Not working:** No controllers, no plant abstraction, no arm kinematics code.",
      "source": "lab-notes/daily/2026-06-14.md"
    },
    {
      "id": "labnote_ed6b58800fac35bb50647863",
      "content": "# Lab note 2026-06-16.md\nSource: lab-notes/daily/2026-06-16.md\n\n---\ntitle: \"2026-06-16 — Controllers + Plant Classes\"\ndate: 2026-06-16\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - controllers\n  - plant\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-16 — Controllers + Plant Classes\n**Last commit:** `6c30dab` — \"refactor: optimize kinematics caching and rename mpc solve method to compute\"\n\n**Files added:**\n- `holonomicmobilerobot.py` — `HolonomicMobileRobot` class (replaces the standalone function)\n- `mpc_lti.py` — `MPC_LTI` class with OSQP QP solver\n- `pid.py` — `PIDCcontroller` class (buggy — missing `_integral` and `_prev_error` initialization)\n\n**Decisions:**\n- **OOP for the robot base.** `HolonomicMobileRobot` with `step()`, `get_state()`, `get_model()`, `set_pose()`. State is `[x, y, theta]`. `step()` takes world-frame velocity, rotates to body frame, computes wheel speeds, integrates state.\n- **MPC_LTI with OSQP.** Full LTI MPC with precomputed dynamics matrices (T_bar, S_bar) and cost matrices (H, F). Uses `osqp.OSQP()` for solving the QP. Terminal cost and constraints.\n- **PID controller.** Basic PID with P, I, D terms and output limits. Implementation is incomplete.\n- **`get_model_matrices()` returns A=I, B=dt\\*I** — base modeled as simple discrete integrator.\n\n**Working:** Base kinematics and state integration. MPC_LTI matrix construction and OSQP solve. PID skeleton.\n**Not working:** PID has uninitialized `_integral`/`_prev_error`, incomplete output clamping. No arm code. No abstract base classes.",
      "source": "lab-notes/daily/2026-06-16.md"
    },
    {
      "id": "labnote_f9be863503b6db8db060378a",
      "content": "# Lab note 2026-06-17.md\nSource: lab-notes/daily/2026-06-17.md\n\n---\ntitle: \"2026-06-17 — Abstract Base Classes + Arm + LQR\"\ndate: 2026-06-17\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - abc\n  - arm\n  - lqr\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-17 — Abstract Base Classes + Arm + LQR\n**Last commit:** `93333c7` — \"feat: extend armrobot with joint limits, offsets, and basic kinematics\"\n\n**Files added:**\n- `components.py` — Abstract base classes: `Controller`, `Plant`, `StateEstimator`\n- `armrobot.py` — `ArmRobot(Plant)` class with joint limits, offsets, and `_homogenous_transform()` stub\n- `lqr.py` — `LQR(Controller)` class using `scipy.linalg.solve_discrete_are`\n\n**Decisions:**\n- **Three abstract base classes:** `Controller` (compute, reset), `Plant` (get_state, get_model, step), `StateEstimator` (estimate, reset). This is the core architectural pattern that persists throughout the project.\n- **ArmRobot as a Plant.** Extends the Plant ABC. Has `num_dof`, `dt`, `state`, `joint_limits`, `joint_offsets`. `get_model()` returns A=I, B=dt\\*I.\n- **LQR via DARE.** Uses the discrete algebraic Riccati equation solver for optimal gain computation.\n- **Arm kinematics stub.** `_homogenous_transform()` started but only handles 'x' axis.\n\n**Working:** ABCs in components.py. LQR controller with DARE solve. ArmRobot skeleton with state tracking.\n**Not working:** Arm forward kinematics incomplete. No Jacobian, no inverse kinematics. PID still buggy. No state estimators.",
      "source": "lab-notes/daily/2026-06-17.md"
    },
    {
      "id": "labnote_2bc64b43ceab2a445afe69bb",
      "content": "# Lab note 2026-06-18.md\nSource: lab-notes/daily/2026-06-18.md\n\n---\ntitle: \"2026-06-18 — Full Arm Kinematics + Documentation\"\ndate: 2026-06-18\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - kinematics\n  - documentation\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-18 — Full Arm Kinematics + Documentation\n**Last commit:** `805fd33` — \"chore: updated README\"\n\n**Files added:**\n- `README.md` — comprehensive documentation with Mermaid architecture diagram, project structure, quick start, and status\n\n**Files significantly evolved:**\n- `armrobot.py` — **complete implementation**: forward kinematics via homogeneous transforms, geometric Jacobian, inverse kinematics via damped pseudoinverse Newton-Raphson, Cartesian `step()` that integrates velocity → pose → IK → joint angles\n\n**Decisions:**\n- **Cartesian arm abstraction.** The arm's `step()` takes a 6D Cartesian velocity twist `[dx, dy, dz, droll, dpitch, dyaw]`, integrates it into a target pose, runs IK internally, and returns joint angles. The controller never touches joint space — it thinks it's controlling a 6D Cartesian plant.\n- **Verified kinematics.** FK via homogeneous transforms, geometric Jacobian verified against numerical differentiation (max error 0.00018), IK with damped pseudoinverse + step clamp.\n- **Full round-trip test.** Cartesian state → IK → FK → state matches.\n- **README documents the full architecture** with Mermaid diagram showing ABCs → concrete implementations → simulation integration.\n\n**Working:** Full arm kinematics (FK, Jacobian, IK). Cartesian step pipeline. All three controllers (PID, MPC_LTI, LQR). Base kinematics. README with architecture docs.\n**Not working:** PID still has initialization bugs. No state estimators. No MuJoCo integration in Python code (simulation is manual via CLI). No combined base+arm control.",
      "source": "lab-notes/daily/2026-06-18.md"
    },
    {
      "id": "labnote_d1a300ffc56b88958760811f",
      "content": "# Lab note 2026-06-20.md\nSource: lab-notes/daily/2026-06-20.md\n\n---\ntitle: \"2026-06-20 — Cleanup\"\ndate: 2026-06-20\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - cleanup\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-20 — Cleanup\n**Commit:** `cccaf2a` — \"fix: remove empty lines and trailing whitespace in holonomic robot matrix calculation\"\n\nMinor cleanup. No functional changes.",
      "source": "lab-notes/daily/2026-06-20.md"
    },
    {
      "id": "labnote_a20b79d988852de847bfc78f",
      "content": "# Lab note 2026-06-23.md\nSource: lab-notes/daily/2026-06-23.md\n\n---\ntitle: \"2026-06-23 — State Estimators + Trajectory Generator\"\ndate: 2026-06-23\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - state-estimators\n  - trajectory\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-23 — State Estimators + Trajectory Generator\n**Last commit:** `bc4e09a` — \"docs: enhance controller/plant/estimator/trajectory base classes with detailed descriptions and usage examples\"\n\n**Files added:**\n- `kalman_filter.py` — `KalmanFilter(StateEstimator)` with full predict-update cycle\n- `luenberger_observer.py` — `LuenbergerObserver(StateEstimator)` with observer gain\n\n**Files evolved:**\n- `components.py` — Added `TrajectoryGenerator` ABC, enhanced docstrings on all ABCs\n\n**Decisions:**\n- **KalmanFilter.** Full discrete-time linear KF with predict (A, B, Q) and update (C, D, R) steps. Tracks posterior state estimate `x_hat` and error covariance `P`. Defaults C to identity, D to zeros.\n- **LuenbergerObserver.** Deterministic observer with configurable gain L. Implements `x̂ₖ₊₁ = A x̂ₖ + B uₖ + L (yₖ − C x̂ₖ − D uₖ)`.\n- **TrajectoryGenerator ABC.** Abstract interface for generating trajectories from start to end over a duration. No concrete implementation yet.\n- **Enhanced ABC docstrings.** All four ABCs (Controller, Plant, StateEstimator, TrajectoryGenerator) now have detailed docstrings with usage examples.\n\n**Working:** Kalman filter (predict-update cycle). Luenberger observer. All four ABCs with documentation.\n**Not working:** No concrete TrajectoryGenerator implementation. No MuJoCo Python integration. No combined base+arm control.",
      "source": "lab-notes/daily/2026-06-23.md"
    },
    {
      "id": "labnote_2c9273d5de597ea7692a0420",
      "content": "# Lab note 2026-06-29-benchmarks.md\nSource: lab-notes/daily/2026-06-29-benchmarks.md\n\n---\ntitle: \"2026-06-29 (Late Late Evening) — JIT + Numba Benchmarks\"\ndate: 2026-06-29\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - benchmarks\n  - jit\n  - numba\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-29 (Late Late Evening) — JIT + Numba Benchmarks\n\n**Context:** Wanted to see if Python 3.14.3's experimental JIT or Numba could speed up the simulation loop.\n\n### Setup\n\n| Config | Python | Env |\n|--------|--------|-----|\n| 3.12.3 plain | 3.12.3 | lebrobot |\n| 3.12.3 + Numba | 3.12.3 + `numba` | lebrobot |\n| 3.14.3 JIT | 3.14.3 `--enable-experimental-jit` | lrtest |\n\nAll deps pinned to identical versions (numpy 2.2.6, scipy 1.17.1, mujoco 3.9.0, etc.).\n\n### JIT Benchmark Results\n\n| Test | 3.12.3 | 3.14.3 JIT | Speedup |\n|------|--------|------------|---------|\n| Chaos test suite (143 tests) | 32.0s | 32.8s | 0.97x |\n| Arm demo (--gif --fast) | 22.2s | 25.0s | 0.88x |\n| Base demo (--gif --fast) | 1m11s | 1m10s | 1.02x |\n| Pure compute (matmul + mujoco) | 1.4s | 1.6s | 0.87x |\n| **Arm IK (1000 calls)** | **4.7s** | **3.5s** | **1.36x 🏆** |\n\n### Numba Benchmark Results\n\n| Test | 3.12.3 plain | 3.12.3+Numba | 3.14.3 JIT | Winner |\n|------|-------------|--------------|------------|--------|\n| DLS kernel (10000 calls) | 134.5ms | **13.4ms** | 80.6ms | **Numba 10x 🏆** |\n| Full mujoco_ik (1000 calls) | 3494.3ms | **3114.0ms** | 3843.0ms | **Numba 1.12x** |\n| numpy matmul (100x 500x500) | 220.6ms | — | 501.9ms | 3.12.3 plain 2.3x |\n| numpy solve (50x 500x500) | 197.0ms | — | **111.1ms** | **3.14 JIT 1.77x** |\n| numpy SVD (20x 500x500) | 3000.0ms | — | **835.2ms** | **3.14 JIT 3.6x 🏆** |\n\n### Profiling: Where does the time go?\n\n**Per-call breakdown of `mujoco_ik()`:**\n| Operation | Time | % of total |\n|-----------|------|-----------|\n| `mj_jac` (Jacobian) | 9.2μs | 3% |\n| DLS solve (numpy) | 32.7μs | 9% |\n| **`mj_forward` (FK update)** | **317.7μs** | **88%** |\n\n**Full simulation loop:**\n| Layer | Wall-clock per step | vs Real-time (20ms) |\n|-------|-------------------|-------------------|\n| Bare physics (`engine.step()`) | 0.23ms | **85x faster** |\n| Full loop (IK + step) | 0.52ms | **38x faster** |\n| Demo with plot + GIF | 73-79ms | **3.7x slower** 😭 |\n\n### Key Takeaways\n\n1. **The simulation is already 85x faster than real-time.** The bottleneck is matplotlib rendering and GIF capture, not the physics or IK.\n2. **The entire stack is C under the hood** — MuJoCo, numpy, scipy, osqp are all C libraries. Python is just glue code. JIT compilers solve a problem that doesn't exist here.\n3. **Numba crushes the DLS kernel (10x)** but only helps 12% on the full IK because `mj_forward` (C, not JIT-able) dominates at 88% of runtime.\n4. **3.14 JIT is inconsistent** — faster on SVD (3.6x) and solve (1.77x) but slower on matmul (0.48x) due to ABI compatibility shims.\n5. **IK iteration sweet spot is 5** — error drops to 0.0007m (sub-millimeter) and going to 20 iterations doesn't improve accuracy, just costs time.\n\n**Verdict:** Not worth switching to 3.14 JIT or adding Numba for this project. The gains are marginal (12% best case) and the bottleneck is matplotlib, not Python bytecode.",
      "source": "lab-notes/daily/2026-06-29-benchmarks.md"
    },
    {
      "id": "labnote_c2ecde5508c6b967c35115ad",
      "content": "# Lab note 2026-06-29-evening.md\nSource: lab-notes/daily/2026-06-29-evening.md\n\n---\ntitle: \"2026-06-29 (Evening) — Arm IK Fix + CLI Flags + MPC + Live Plot\"\ndate: 2026-06-29\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - arm-ik\n  - cli\n  - mpc\n  - live-plot\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-29 (Evening) — Arm IK Fix + CLI Flags + MPC + Live Plot\n**No new commits** (all changes in working tree)\n\n**What changed:**\n\n### Arm Control Fix\nThe arm was **not actually moving up** — the logging was reading `sim.arm.get_state()` which returns `self.state`, only updated by `sim.arm.step()`. Since the demo used `sim.engine.set_arm_ctrl()` directly (bypassing `sim.arm.step()`), the logged state was **stale** — always showing z=0.141.\n\n**Root cause chain:**\n1. `lekiwi_sim.py` `reset()` set free joint quaternion to `[0,0,0,0]` (invalid) → garbage EE positions\n2. Fallback FK model in `armrobot.py` was completely disconnected from MuJoCo's mesh — FK at q=0 gave z=0.031m, MuJoCo's real mesh gave z=0.141m\n3. `sim.arm.get_state()` returned stale data because `set_arm_ctrl()` doesn't update `self.state`\n\n**Fixes:**\n- `lekiwi_sim.py`: `reset()` now sets free joint quaternion to `[1,0,0,0]` (identity)\n- `capture_gif.py`: `true_ee` reads directly from `sim.engine.data.xpos[ee_body_id]` instead of `sim.arm.get_state()`\n- `capture_gif.py`: Arm control uses **iterative damped least-squares IK** on MuJoCo's real Jacobian (converges in ~8 iterations for 0.16m error)\n- IK restores original qpos after computation so position servos have error to track\n- Removed all broken fallback FK/IK code from `capture_gif.py`\n\n**Result:** Arm now tracks with ~0.04m mean error. Reaches z=0.287 (target 0.300) and z=0.044 (target 0.050).\n\n### CLI Flags Added\n- `--controller lqr|mpc` — choose base controller (LQR default, MPC with horizon=15 + velocity constraints)\n- `--trajectory straight|triangle` — choose base trajectory (straight line or triangle: forward → diagonal → back to origin)\n- `--gif` — headless GIF capture\n- `--fast` — fewer frames for faster rendering\n\n### MPC Integration\n- MPC_LTI from `mpc_lti.py` wired into demo with same Q/R matrices as LQR\n- Velocity constraints: vx, vy ∈ [-0.5, 0.5], ω ∈ [-1.0, 1.0]\n- Plot title dynamically shows \"LQR Control Effort\" or \"MPC Control Effort\"\n\n### Live Plot Improvements\n- 3×2 layout: base tracking (ref/noisy/est/true), base innovation, arm tracking, arm innovation, base control effort, arm position error\n- Sensor noise + Luenberger observer (base) + EMA estimator (arm)\n- Colored waypoint markers in 3D scene\n- Dark theme\n\n**Working:** Both controllers (LQR/MPC), both trajectories (straight/triangle), arm tracks EE targets with ~0.04m error, live viewer + growing plot, GIF capture.\n**Not working:** Arm position servos (kp=50) are slow to converge for large displacements. No combined state-space. No real pick-and-place (no object in scene).\n\n**Linear issues updated:**\n- **ADI-45** (MuJoCo Closed-Loop Simulation) — renamed to reflect LQR/MPC + Arm IK, kept In Progress\n- **ADI-56** (Arm IK Fix) — created + marked Done\n- **ADI-57** (CLI Flags + MPC Integration) — created + marked Done\n- **ADI-58** (Live Plot + Estimators + Waypoints) — created + marked Done\n- **ADI-59** (Arm Position Servo Tracking Speed) — created, in Backlog",
      "source": "lab-notes/daily/2026-06-29-evening.md"
    },
    {
      "id": "labnote_c12b653d2b90b3a5a2e2bc99",
      "content": "# Lab note 2026-06-29-late-evening.md\nSource: lab-notes/daily/2026-06-29-late-evening.md\n\n---\ntitle: \"2026-06-29 (Late Evening) — DRY Refactor + Chaos Tests + Detached HEAD\"\ndate: 2026-06-29\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - refactor\n  - tests\n  - git\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-29 (Late Evening) — DRY Refactor + Chaos Tests + Detached HEAD\n\n**Commits pushed:** `6f3a094..668ec0e` (9 orphaned commits rescued from detached HEAD)\n\n### DRY Refactor of `armrobot.py`\n\nEliminated 5 duplication sites:\n\n| What | Before | After |\n|---|---|---|\n| `self.state = np.zeros(6)` | Set twice in `__init__` | Once |\n| `get_state()` | Two identical `return self.state.copy()` branches | One line |\n| EE position `xpos[ee_body_id][0..2]` | Repeated in `physics_engine()` and `step()` | `_get_ee_pos()` helper |\n| Jacobian alloc + `mj_jac` + column slice | Repeated in `step()` and `mujoco_ik()` | `_get_ee_jacobian()` helper |\n| `arm_jac_start` logic | Repeated in `step()` and `mujoco_ik()` | Cached in `physics_engine()` |\n\n**`step()` now delegates to `mujoco_ik()`** — integrates `u[:3] * dt` to a target EE position, then calls `mujoco_ik()` for damped least-squares convergence. Both paths converge to the same IK solver.\n\n### Chaos Tests\n\n**143/143 passed** — threw every degenerate input at all concrete implementations:\n\n- **ArmRobot (43 tests):** NaN, Inf, wrong shapes, empty arrays, complex numbers, strings, negative dt, zero dt, 1000-step stress tests, sawtooth patterns\n- **HolonomicMobileRobot (25 tests):** Same battery — all caught `__init__` signature mismatch (missing `radius_robots`, `gamma`, `radius_wheels` params)\n- **PIDController (23 tests):** NaN targets, zero/negative/huge gains, scalar vs vector, saturation with unequal limits, only-min/only-max clamping\n- **MPC_LTI (21 tests):** NaN/Inf states, horizon=0, horizon=100, singular A, zero B, zero Q, zero R, mismatched dims, no constraints\n- **MPC_LTI_DeltaU (8 tests):** NaN/Inf, no u_prev, wrong u_prev shape, zero/huge delta penalty\n- **LuenbergerObserver (23 tests):** NaN/Inf, zero/negative gain, singular A, zero C, partial observation, 2D/6D systems\n- **Cross-component (5 tests):** Arm+PID loop, Base+PID loop, Base+MPC loop, Base+Observer+MPC loop, Arm+Observer+PID loop\n\n### Detached HEAD Bug\n\nThe lerobot-mpc-lekiwi repo was in detached HEAD state — the auto-commit script (`ai_commit.py`, runs every 30 min) was committing locally but `git push` silently failed because there was no branch to push from. **9 commits** were orphaned between `6f3a094` and `2af827e`.\n\n**Root cause:** A `git checkout <hash>` during the session left the repo in detached HEAD. The auto-commit script never checked what branch it was on.\n\n**Fix:** Patched `ai_commit.py` to detect detached HEAD and checkout `main` before committing:\n```python\nbranch = run_git_command(\"git rev-parse --abbrev-ref HEAD\", cwd=repo_path)\nif not branch or branch == \"HEAD\":\n    run_git_command(\"git checkout main\", cwd=repo_path)\n```\n\n### Tracking Error Bug\n\n`get_state()` was returning `self.state.copy()` — a cache only updated inside `step()`. Since the demo calls `mujoco_ik()` directly (not `step()`), the cache was frozen at the home position. The plot showed ~0.04m+ error when the arm was actually tracking at ~0.0004m.\n\n**Fix:** `get_state()` now reads directly from MuJoCo's `xpos` when an engine is attached.\n\n### Future Idea: Simulation Loop Abstraction\n\nThe simulation loop boilerplate is duplicated across `capture_gif.py` and `demo_base_movement.py`:\n\n```\nsetup engine + plants + controllers + plot\nfor step in range(total_steps):\n    compute control\n    step physics\n    log data\n    update plot\n    capture frame\nteardown\n```\n\nCould be abstracted into a `SimulationRunner` class where demos just configure controllers, loggers, and plot layout — ~50 lines instead of 300+. Not urgent, but the pattern is ripe for extraction when a third demo appears.",
      "source": "lab-notes/daily/2026-06-29-late-evening.md"
    },
    {
      "id": "labnote_75ecf3ccce27bc8c6cb96890",
      "content": "# Lab note 2026-06-29.md\nSource: lab-notes/daily/2026-06-29.md\n\n---\ntitle: \"2026-06-29 — MuJoCo Integration + Demos + Tests\"\ndate: 2026-06-29\ntags:\n  - lekiwi\n  - mpc\n  - journal\n  - daily\n  - mujoco\n  - demos\n  - tests\ncategory: lekiwi\nstatus: live\n---\n\n## 2026-06-29 — MuJoCo Integration + Demos + Tests\n**Last commit:** `6f3a094` — \"feat: implement pick-and-place demo with LQR base control and arm trajectory planning\"\n\n**Files added:**\n- `lekiwi_sim.py` — `MuJoCoEngine` and `LeKiwiSim` classes for Python-based MuJoCo simulation\n- `capture_demo.py` — pick-and-place demo with 8-phase scripting\n- `capture_gif.py` — GIF capture demo with LQR base control + cubic trajectory planning\n- `test_pick_and_place.py` — 6 real tests for the pick-and-place pipeline\n- `lekiwi_demo.gif` — output GIF\n- `.python-version` — Python 3.12.3\n- `.zed/settings.json` — Zed editor config\n\n**Decisions:**\n- **MuJoCoEngine as a physics backend, NOT a Plant.** The engine is a low-level MuJoCo wrapper. `ArmRobot` and `HolonomicMobileRobot` each hold a reference to the engine and read/write their respective slices of the shared state. Controllers talk to the robot classes, never to MuJoCo directly.\n- **LeKiwiSim convenience wrapper.** Creates a shared `MuJoCoEngine` and instantiates `ArmRobot` + `HolonomicMobileRobot` with it. Provides `reset()`, `step()`, `get_state()`.\n- **LQR for base position tracking.** Q=diag(100,100,50), R=diag(0.1,0.1,0.1).\n- **Cubic trajectory for arm.** Simple `Trajectory` class (not the ABC) with precomputed keyframes.\n- **EGL headless rendering.** Both demos use `MUJOCO_GL=egl` for headless GIF capture.\n- **Comprehensive test suite.** 6 tests covering base driving, diagonal movement, arm reaching, gripper open/close, full pick-and-place sequence, and reset.\n\n**Working:** Full end-to-end pick-and-place demo with MuJoCo physics, LQR base control, arm trajectory planning, GIF capture. All 6 tests pass.\n**Not working:** Base is welded to world in MuJoCo (no free joint) — base pose is always `[0,0,0]` in MuJoCo, with kinematic state tracked separately in `HolonomicMobileRobot.state`. Wheel visual roll is manually updated via `_target_wheel_delta`. No combined state-space (base + arm coupling). No Shinro IDE integration.",
      "source": "lab-notes/daily/2026-06-29.md"
    },
    {
      "id": "labnote_17e6a758b64102275ac4d99d",
      "content": "# Lab note 2026-07-05.md\nSource: lab-notes/daily/2026-07-05.md\n\n---\ntags: [lab-note, auto, git]\ndate: 2026-07-05\ncategory: lab-note\nstatus: active\n---\n\n# 2026-07-05 — Git Activity\n\n## Commits\n\n### Commit: feat: implement lekiwi robot base kinematics, arm control, and core control arch\n*21:30 · Adil Faisal*\n\n**Changed files:** lab-notes/daily/2026-06-10.md, lab-notes/daily/2026-06-14.md, lab-notes/daily/2026-06-16.md, lab-notes/daily/2026-06-17.md, lab-notes/daily/2026-06-18.md, lab-notes/daily/2026-06-20.md, lab-notes/daily/2026-06-23.md, lab-notes/daily/2026-06-29-benchmarks.md, lab-notes/daily/2026-06-29-evening.md, lab-notes/daily/2026-06-29-late-evening.md...\n\n**Index:** 81 files, 11 vault notes\n\n### Commit: docs: add daily lab note for 2026-07-05 with lekiwi robot progress summary\n*22:00 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, lab-notes/daily/2026-07-05.md\n\n**Index:** 82 files, 12 vault notes\n\n### Commit: docs: add daily lab note for 2026-07-05 with lekiwi robot progress\n*22:30 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, lab-notes/daily/2026-07-05.md\n\n**Index:** 82 files, 12 vault notes",
      "source": "lab-notes/daily/2026-07-05.md"
    },
    {
      "id": "labnote_7166ce4fb6f35ff5e8acb344",
      "content": "# Lab note 2026-07-08.md\nSource: lab-notes/daily/2026-07-08.md\n\n---\ntags: [lab-note, auto, git]\ndate: 2026-07-08\ncategory: lab-note\nstatus: active\n---\n\n# 2026-07-08 — Major Architecture Refactor\n\n## Summary\n\nToday we did a ground-up architectural refactor of the entire codebase. The goal was to make the framework fully generic — any robot, any controller, any estimator, any trajectory generator, any physics engine — all config-driven with zero hardcoded wiring.\n\n## What Changed\n\n### PhysicsEngine ABC (`physics_engine/`)\n- Extracted `PhysicsEngine` abstract base class from `components.py` into `physics_engine/base.py`-adjacent\n- `MuJoCoEngine` now implements `PhysicsEngine` — name-based joint/body access, no hardcoded LeKiwi slices\n- `ArmRobot` no longer imports `mujoco` — talks to the engine through the protocol\n\n### Registry Pattern (`factories/registry.py`)\n- Four registries: `_CONTROLLER_REGISTRY`, `_ESTIMATOR_REGISTRY`, `_TRAJECTORY_REGISTRY`, `_PLANT_REGISTRY`\n- Each concrete class registers itself with a decorator (`@register_controller(\"LQR\")`, `@register_plant(\"ArmRobot\")`, etc.)\n- Factories are now 3-line generics: lookup `from_config` → instantiate\n- Adding a new component: just add the decorator + `from_config` + TOML config — no factory code changes\n\n### TOML-Driven Configuration\n- All configs moved from YAML to TOML (built-in `tomllib`, no dependency)\n- `robot_config.toml` — defines the robot model, joint groups, plants\n- `configs/controllers/*.toml` — controller type + weights\n- `configs/estimators/*.toml` — estimator type + gains\n- `configs/trajectories/*.toml` — trajectory type + waypoints\n\n### New Modules\n- `simulation/robotsim.py` — `RobotSim` generic factory (reads TOML, creates engine + plants via registry)\n- `factories/` — registry and three factories\n- `demos/` — four demo scripts (simple, arm, base tracking, pick-and-place)\n- `deprecated/` — old demo scripts moved as-is\n- `scripts/generate_robot_config.py` — auto-generates `robot_config.toml` from MJCF XML\n\n### Key Files Moved\n- `RobotSim` from `lekiwi_sim.py` → `simulation/robotsim.py`\n- `MuJoCoEngine` from `lekiwi_sim.py` → `physics_engine/mujoco.py`\n- `PhysicsEngine` ABC from `components.py` → `physics_engine/__init__.py` (re-exported)\n\n### Removed (Dead Code)\n- `run_simulation()` — zero callers\n- `MuJoCoEngine.get_drive_qpos()`, `set_arm_ctrl()`, `set_drive_ctrl()` — zero callers\n- `HolonomicMobileRobot.set_pose()` — zero callers\n- `lekiwi-sim/so_arm100.xml` — unused XML referencing nonexistent meshes\n\n## Registry Contents (all auto-discovered)\n\n| Type | Registered |\n|------|-----------|\n| Controllers | LQR, PID, MPC_DeltaU, MPC_LTI |\n| Estimators | KalmanFilter, LuenbergerObserver |\n| Trajectories | cubic_segments, quintic_segments, waypoints, phase_list |\n| Plants | ArmRobot, HolonomicMobileRobot |\n\n## Commits\n\n### Commit: feat: add PhysicsEngine abstract base class with simulation interface methods\n*12:00 · Adil Faisal*\n\n**Changed files:** components.py\n\n**Index:** 82 files, 12 vault notes\n\n### Commit: refactor: streamline capture_demo and capture_gif by using RobotSim, remove unus\n*12:30 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, capture_demo.py, capture_gif.py, components.py, demo_base_movement.py, lab-notes/daily/2026-07-08.md, lekiwi_arm_demo.gif, lekiwi_demo.gif, lekiwi_sim.py, physics_engine/__init__.py...\n\n**Index:** 86 files, 13 vault notes\n\n### Commit: feat(scripts): add generate_robot_config utility to create robot_config.yaml fro\n*13:00 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, lab-notes/daily/2026-07-08.md, scripts/generate_robot_config.py\n\n**Index:** 87 files, 13 vault notes\n\n### Commit: feat(factory): add controller, estimator, and trajectory factories with YAML con\n*13:30 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, configs/controllers/lqr_base.yaml, configs/controllers/mpc_base.yaml, deprecated/capture_demo.py, deprecated/capture_gif.py, deprecated/demo_base_movement.py, factories/__init__.py, factories/controller_factory.py, factories/estimator_factory.py, factories/trajectory_factory.py...\n\n**Index:** 93 files, 13 vault notes\n\n### Commit: feat: add Luenberger observer config, new trajectory YAML files, register contro\n*14:00 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, configs/estimators/luenberger_base.yaml, configs/trajectories/arm_extension.yaml, configs/trajectories/base_straight.yaml, configs/trajectories/base_triangle.yaml, configs/trajectories/pick_and_place.yaml, controllers/lqr.py, controllers/mpc_lti.py, controllers/pid.py, demos/__init__.py...\n\n**Index:** 104 files, 13 vault notes\n\n### Commit: feat: add simple demo script and extend factories with plant registry and regist\n*14:30 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, demos/demo_simple.py, factories/__init__.py, factories/registry.py, factories/trajectory_factory.py, lab-notes/daily/2026-07-08.md, lekiwi_arm_demo.gif, lekiwi_demo.gif, lekiwi_sim.py, plants/armrobot.py...\n\n**Index:** 105 files, 13 vault notes\n\n### Commit: update config from yaml to toml\n*14:56 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, README.md, configs/controllers/lqr_base.toml, configs/controllers/lqr_base.yaml, configs/controllers/mpc_base.toml, configs/controllers/mpc_base.yaml, configs/estimators/luenberger_base.toml, configs/estimators/luenberger_base.yaml, configs/trajectories/arm_extension.toml, configs/trajectories/arm_extension.yaml...\n\n**Index:** 107 files, 13 vault notes\n\n### Commit: chore: add requirements.txt with project dependencies\n*15:30 · Adil Faisal*\n\n**Changed files:** requirements.txt\n\n**Index:** 108 files, 13 vault notes\n\n### Commit: feat: support start/end velocity and acceleration in cubic and quintic polynomia\n*16:30 · Adil Faisal*\n\n**Changed files:** trajectories/cubic_polynomial.py, trajectories/quintic_polynomial.py\n\n**Index:** 108 files, 13 vault notes\n\n### Commit: feat(controller): add LeRobot diffusion adapter and configuration support\n*19:30 · Adil Faisal*\n\n**Changed files:** configs/controllers/lerobot_diffusion.toml, controllers/__init__.py, controllers/lerobot_adapter.py\n\n**Index:** 110 files, 13 vault notes\n\n### Commit: refactor: add backend-agnostic array operations across all components\n*20:00 · Adil Faisal*\n\n**Changed files:** utils/array_backend.py, components.py, controllers/lqr.py, controllers/pid.py, controllers/mpc_lti.py, controllers/lerobot_adapter.py, estimators/kalman_filter.py, estimators/luenberger_observer.py, plants/holonomicmobilerobot.py, plants/armrobot.py, trajectories/cubic_polynomial.py, trajectories/quintic_polynomial.py, factories/controller_factory.py, factories/estimator_factory.py, factories/trajectory_factory.py, README.md\n\n**Summary:**\n- Added `utils/array_backend.py` with `ArrayBackend` ABC, `NumpyBackend`, and `TorchBackend` (22 array ops each)\n- Every component now takes an optional `backend` parameter (defaults to `NumpyBackend`)\n- All `np.xxx` calls replaced with `self.bk.xxx()` — ~72 substitutions across 10 files\n- `PhysicsEngine` ABC has a `backend` property that propagates to plants\n- Factories pass `backend` through `from_config()` and `create()`\n- `LeRobotDiffusionAdapter` handles both numpy and torch inputs\n- Two numpy-specific workarounds: DARE (LQR, one-time) and OSQP (MPC, per-step)\n- Integration tests pass identically (16/25, same pre-existing arm failures)\n\n### Commit: refactor: add backend-agnostic array operations across all components\n*19:57 · Adil Faisal*\n\n**Changed files:** README.md, components.py, controllers/lerobot_adapter.py, controllers/lqr.py, controllers/mpc_lti.py, controllers/pid.py, estimators/kalman_filter.py, estimators/luenberger_observer.py, factories/controller_factory.py, factories/estimator_factory.py...\n\n**Index:** 112 files, 13 vault notes\n\n### Commit: refactor: add backend-agnostic array operations across all components\n*20:00 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, lab-notes/daily/2026-07-08.md, lekiwi_demo.gif\n\n**Index:** 112 files, 13 vault notes\n\n### Commit: docs: add Google-style docstrings to all components\n*20:01 · Adil Faisal*\n\n**Changed files:** .codebase/codebase_index.db, controllers/lerobot_adapter.py, controllers/lqr.py, controllers/mpc_lti.py, controllers/pid.py, estimators/kalman_filter.py, estimators/luenberger_observer.py, lab-notes/daily/2026-07-08.md, plants/armrobot.py, plants/holonomicmobilerobot.py...\n\n**Index:** 112 files, 13 vault notes",
      "source": "lab-notes/daily/2026-07-08.md"
    },
    {
      "id": "labnote_a38d4dc0c548c646a930030a",
      "content": "# Lab note 2026-07-23.md\nSource: lab-notes/daily/2026-07-23.md\n\n# Lab Notes — 2026-07-23\n\n<<<<<<< HEAD\n### 2026-07-23 19:22 UTC — initial\n=======\n### 2026-07-23 20:00 UTC — initial\n>>>>>>> cfd41b1 (chore: add run_ci_local.txt to .gitignore)\n\n\n\n### 2026-07-23 22:03 UTC — update\n\n\n\n### 2026-07-23 21:00 UTC — update\n\n\n\n### 2026-07-23 23:00 UTC — update",
      "source": "lab-notes/daily/2026-07-23.md"
    },
    {
      "id": "labnote_4eb691649ecf16b370866692",
      "content": "# Lab note 2026-07-27.md\nSource: lab-notes/daily/2026-07-27.md\n\n# Lab Notes — 2026-07-27\n\n### 2026-07-27 22:30 UTC — initial\n\n**Change: new-plants-and-generator**\n\nAdded InvertedPendulum and CartPole benchmark plants with standalone analytical dynamics and optional MuJoCo engine mode. Both implement `dynamics()` returning continuous-time dx/dt — the hook for future nonlinear controllers (NMPC, MPPI, sliding mode) and estimators (EKF, UKF).\n\n**New plants:**\n- `plants/inverted_pendulum.py` — 2D state [θ, θ̇], 1D torque control, semi-implicit Euler, linearized model around upright, configurable damping and state bounds\n- `plants/cartpole.py` — 4D state [x, ẋ, θ, θ̇], 1D force control, coupled dynamics, linearized model, configurable track limits\n- `plants/quadrotor.py` — placeholder stub for future implementation\n\n**Generic XML config generator:**\n- Refactored `scripts/generate_robot_config.py` to use a detector registry instead of hardcoded LeKiwi logic\n- Added `_PLANT_DETECTOR_REGISTRY` and `@register_plant_detector` decorator to `factories/registry.py`\n- Each plant registers a detector that inspects XML structure (joint types, actuator types) to auto-detect plant type\n- Three-tier fallback: heuristic → XML annotation → CLI `--type` flag\n- Supports combined robots (e.g., LeKiwi → ArmRobot + HolonomicMobileRobot) and batch directory mode\n- Physical parameter extraction from XML (masses, joint limits, damping, actuator ranges, gravity)\n\n**MuJoCo models:**\n- `models/pendulum.xml` — hinge joint, capsule rod + sphere mass, torque actuator\n- `models/cartpole.xml` — slider + hinge, box cart + capsule pole, force actuator\n\n**Tests:**\n- TestInvertedPendulum: standalone dynamics, linearized model, state bounds, from_config (16 tests)\n- TestCartPole: standalone dynamics, linearized model, track limits, from_config (14 tests)\n- TestConfigGenerator: pendulum/cartpole/LeKiwi detection, batch mode, unknown XML fallback, CLI override (9 tests)\n- All 663 tests pass across the full suite (including CI via act on 3.12, 3.13, 3.14)\n\n### 2026-07-27 23:00 UTC — update\n\nAdded detailed Google-style docstrings to both new plants matching the existing codebase convention. Fixed `physics_engine(None)` to reset backend and state properly.\n\n### 2026-07-27 23:30 UTC — update",
      "source": "lab-notes/daily/2026-07-27.md"
    },
    {
      "id": "labnote_0a9bebb2a791e844b2f33a31",
      "content": "# Lab note 2026-07-28.md\nSource: lab-notes/daily/2026-07-28.md\n\n# Lab Notes — 2026-07-28\n\n### 2026-07-28 17:30 UTC — initial\n\n\n\n### 2026-07-28 18:00 UTC — update\n\n\n\n### 2026-07-28 19:30 UTC — update\n\n\n\n### 2026-07-28 20:00 UTC — update\n\n\n\n### 2026-07-28 20:30 UTC — update\n\n\n\n### 2026-07-28 21:00 UTC — update\n\n\n\n### 2026-07-28 21:30 UTC — update\n\n\n\n### 2026-07-28 22:30 UTC — update\n\n\n\n### 2026-07-28 22:40 UTC — update\n\n\n\n### 2026-07-28 22:42 UTC — update\n\n\n\n### 2026-07-28 22:44 UTC — update\n\n\n\n### 2026-07-29 00:30 UTC — update",
      "source": "lab-notes/daily/2026-07-28.md"
    },
    {
      "id": "labnote_992cb694546841b27019a2c5",
      "content": "# Lab note 2026-07-29.md\nSource: lab-notes/daily/2026-07-29.md\n\n# Lab Notes — 2026-07-29\n\n### 2026-07-29 04:30 UTC — initial",
      "source": "lab-notes/daily/2026-07-29.md"
    },
    {
      "id": "labnote_fd383caa786d43fc8e74a0b1",
      "content": "# Lab note 2026-08-01.md\nSource: lab-notes/daily/2026-08-01.md\n\n# Lab Notes — 2026-08-01\n\n### 2026-08-01 22:25 UTC — initial\n\n\n\n### 2026-08-01 22:25 UTC — update\n\n\n\n### 2026-08-01 22:26 UTC — update\n\n\n\n### 2026-08-01 22:26 UTC — update\n\n\n\n### 2026-08-01 22:27 UTC — update",
      "source": "lab-notes/daily/2026-08-01.md"
    },
    {
      "id": "labnote_9bd812776f2e2fa2430d6675",
      "content": "# Lab note 2026-08-05.md\nSource: lab-notes/daily/2026-08-05.md\n\n# Lab Notes — 2026-08-05\n\n### 2026-08-05 13:22 UTC — initial\n\n\n\n### 2026-08-05 13:26 UTC — update\n\n\n\n### 2026-08-05 13:26 UTC — update\n\n\n\n### 2026-08-05 13:30 UTC — update\n\n\n\n### 2026-08-05 13:39 UTC — update\n\n\n\n### 2026-08-05 13:42 UTC — update\n\n\n\n### 2026-08-05 13:50 UTC — update\n\n\n\n### 2026-08-05 14:00 UTC — update\n\n\n\n### 2026-08-05 14:06 UTC — update\n\n\n\n### 2026-08-05 14:08 UTC — update\n\n\n\n### 2026-08-05 14:30 UTC — update\n\n\n\n### 2026-08-05 15:00 UTC — update\n\n\n\n### 2026-08-05 15:10 UTC — integration test environment brought to passing + CI independence\n\n**What changed**\n\nCompleted and verified the full-loop integration test environment under `tests/integration/` (scaffold first landed in `ba2f495` on this branch; this session fixed the blockers and finished the wiring).\n\n- **`tests/integration/`** — 5 test modules + `conftest.py` + `helpers/` + 4 scenario TOMLs, all gated behind `pytest.mark.integration`:\n  - `test_base_tracking.py` — full closed loop Trajectory→Controller→Estimator→HolonomicMobileRobot→MuJoCo; parametrized over LQR/LQR+Luenberger/MPC_LTI/MPC_DeltaU.\n  - `test_arm_cartesian.py` — PID+Kalman tracking a 6D end-effector lift schedule on the SO-ARM100.\n  - `test_pick_and_place.py` — `phase_list` multi-plant feedforward (base+arm+jaw) through `RobotSim`.\n  - `test_adversarial_integration.py` — NaN measurement injected at step 50 must raise, not corrupt control.\n  - `test_scenario_factory.py` — construction validation + dimension-mismatch errors.\n- **`helpers/scenario_runner.py`** — `run_scenario` (closed loop, single plant) and `run_phase_schedule` (feedforward multi-signal). Raises `ValueError` when the estimator propagates NaN/Inf — this is the agreed adversarial contract.\n- **`factories/scenario_factory.py`** — clear `KeyError` for unknown plant names; optional controller/estimator sections (feedforward pick-and-place scenarios).\n- **`configs/controllers/mpc_lti_base.toml`** — fixed a latent constraint-dimension bug: F = [I; −I] is 6 rows, so upper/lower bounds must be length 6 (was 3 → OSQP `Incorrect dimension of l`).\n- **`configs/estimators/kalman_arm.toml`, `configs/plants/holonomic_base.toml`, `configs/trajectories/arm_lift.toml`** — arm estimator, base plant, and 6D arm lift configs.\n- **CI independence**: `pyproject.toml` has `addopts = \"-m 'not integration'\"` so any `pytest` invocation skips the slow MuJoCo suite by default; `make test-integration` (or `run_tests.py --int`) opts in with `--override-ini=\"addopts=\"`. `tests/integration/conftest.py` adds a second safety net — `pytest.importorskip(\"mujoco\")` + asset-existence check.\n- **Removed** root `test_pick_and_place.py` (legacy, non-pytest, bypassed controllers/estimators) and the `test-pick-and-place` Makefile target. Old behavior is subsumed by `tests/integration/test_pick_and_place.py`.\n- `demos/helpers.py` now hosts the shared `inject_free_joint` + `load_model_assets` (factored from the two demos that previously duplicated it).\n\n**Key decisions**\n- Adversarial policy: NaN/Inf measurements RAISE (runner throws `ValueError`) rather than silently corrupting control. Verified the Kalman filter currently propagates NaN — the integration test pins the raise behavior as the contract.\n- Pick-and-place is feedforward (phase_list is a script of velocity setpoints), so it has no feedback controller/estimator; the schedule itself is the control.\n\n**Test results**\n- `make test-integration`: 32 passed (~28–45s; MuJoCo + LeKiwi assets required).\n- `make test`: 744 passed, 33 deselected (32 integration + 1 slow MPC timeout) in ~3:44 — CI untouched, stays green.\n- Empirical calibrations: base loop steady-state error ~7.6mm (LQR), ~5.5mm (MPC_DeltaU); arm EE ~3cm; pick-and-place drive reaches x≈1.35.\n\n**Files touched**\n`Makefile`, `run_tests.py`, `factories/scenario_factory.py`, `configs/controllers/mpc_lti_base.toml`, root `test_pick_and_place.py` (deleted). (Integration scaffold files + demos/helpers.py + pyproject.toml were already committed in `ba2f495`.)\n\n### 2026-08-05 15:30 UTC — update\n\n\n\n### 2026-08-05 16:00 UTC — update\n\n\n\n### 2026-08-05 16:30 UTC — update\n\n\n\n### 2026-08-05 17:59 UTC — update\n\n\n\n### 2026-08-05 18:30 UTC — update\n\n\n\n### 2026-08-05 19:00 UTC — update\n\n\n\n### 2026-08-05 19:30 UTC — packaging: `shinro` becomes an installable Python package\n\nRestructured the repo into a `src/shinro/` package so it can be pip-installed\nand continuously released. This was the last step of the \"is it ready for\npackaging treatment?\" review — it was ready: the registry/factory/ABC pattern\nand per-component `__init__.py` exports already existed, so this was a layout +\nbuild-metadata change, not a code rewrite.\n\n**What changed**\n- Moved `components.py` + `controllers/`, `estimators/`, `plants/`,\n  `trajectories/`, `utils/`, `factories/`, `simulation/`, `physics_engine/`,\n  and `configs/` under `src/shinro/`. `configs/` (incl. a shipped default\n  `robot_config.toml`) now live in the package so they ship with the wheel.\n- Moved `shinro_mcp_server.py` → `src/shinro/mcp/server.py` with a `main()`\n  entry point; installed as the `shinro-mcp` console command.\n- Rewrote `pyproject.toml`: `[project]` metadata, `setuptools` + `setuptools-scm`\n  build backend, `dynamic = [\"version\"]` from git tags, dependency ranges\n  (`mcp>=1.28,<2`), optional extras (`mujoco`, `torch`, `lerobot`, `media`,\n  `dev`), `[tool.setuptools.packages.find] where=[\"src\"]`, and\n  `package-data` for `configs/**/*.toml`.\n- Added `utils/config_resolver.py`: factories still accept path strings, but\n  relative `configs/...` names and bare filenames now resolve against the\n  packaged configs via `importlib.resources`, falling back to CWD. Verified it\n  works from an arbitrary CWD (post-install) and from a source checkout.\n- Rewrote all internal imports from top-level (`from controllers...`) to\n  `from shinro.controllers...`. `lekiwi_sim.py` stays at the repo root as a\n  legacy, non-packaged script (imports patched).\n- CI: added `pip install -e .`, version/import smoke checks, and updated the\n  pyright path. `Makefile`: added `install` and `build` targets.\n- `.gitignore` ignores the setuptools-scm-generated `src/shinro/_version.py`;\n  added `MANIFEST.in`.\n\n**Key decisions**\n- `mcp` pinned `<2` — mcp 2.x removed `mcp.server.fastmcp`; caught by a fresh\n  venv install, not by the pinned `requirements.txt`.\n- Configs are resolved at the factory `__init__` via `resolve_config_path`,\n  so the public factory string API is unchanged — existing tests/demos keep\n  passing `\"configs/controllers/lqr_base.toml\"`.\n- `simulation`/`physics_engine` import `mujoco` eagerly and therefore require\n  the `[mujoco]` extra; the core package imports cleanly without it.\n\n**Test results**\n- `pip install -e .` clean; `import shinro` → `0.0.1.dev138+gc39d9b551`.\n- Wheel built with `python -m build`; contains 19 config TOMLs; installed into\n  a fresh venv and `ControllerFactory(\"configs/controllers/lqr_base.toml\")`\n  resolved against the packaged config.\n- `pytest tests/` (default, minus slow MPC timeout): 525 passed, 219 skipped,\n  33 deselected. MCP functional suite: 37 passed. Integration suite unchanged.\n- `ruff check .` clean; `pyright` on the pyright-scoped modules: 0 errors.\n\n**Files touched**\n`pyproject.toml`, `MANIFEST.in`, `Makefile`, `.gitignore`, `requirements-dev.txt`,\n`README.md`, `.github/workflows/ci.yml`, all `src/shinro/**` (moved + imports),\n`src/shinro/utils/config_resolver.py` (new), `src/shinro/mcp/server.py` (moved +\n`main()`), `tests/test_mcp_server*.py` (imports), `lekiwi_sim.py`, `demos/*`,\n`scripts/generate_robot_config.py`.\n\n\n\n### 2026-08-05 19:54 UTC — update\n\n\n\n### 2026-08-05 20:30 UTC — update\n\n\n\n### 2026-08-05 21:00 UTC — update\n\n\n\n### 2026-08-05 21:30 UTC — CI diagnosis and fixes\n\nInvestigated three failing GitHub Actions workflows on push to `main`.\n\n**What changed**\n\n- **`src/shinro/controllers/mppi.py`** — was committed in `581b338` (and then\n  extended in `1b6f096`) with a truncated, syntactically invalid body\n  (`self.` dangling at end of `__init__`, no `compute`). This broke the CI\n  pyright step (`Expected indented block`). Completed the stub: store all\n  constructor params (`u_min`/`u_max`, `noise_sigma` default), add a\n  `compute()` raising `NotImplementedError`, and fixed import ordering.\n  MPPI is still not wired into `controllers/__init__.py` or the registry —\n  the stub just needs to compile so CI passes.\n- **`tests/test_mcp_server_functional.py`** — the `server` fixture captured\n  stderr but never read it, so when the MCP subprocess died at startup the\n  real traceback was invisible (37 `JSONDecodeError` setup errors on the\n  test.yml runner). `_request` now raises an `AssertionError` including\n  `proc.stderr` when the server returns an empty line, so the next CI run\n  will surface the actual crash cause.\n- **`.github/workflows/update-labnotes-and-index.yml` → renamed\n  `update-labnotes.yml`** — removed the \"Reindex codebase\" step and the\n  `git add .codebase/codebase_index.db` (that file is gitignored and never\n  created on the runner, so `git add` exited 128 on every push). Lab-note\n  generation + commit retained. Per AGENTS.md the codebase index is no\n  longer maintained.\n\n**Key decisions**\n\n- MPPI: minimal compilable stub, not a full implementation — unblocks CI\n  now; a real sampling-based rollout lands in a follow-up.\n- Left the pre-existing `test.yml` MCP functional flakiness diagnosis\n  pending: the fixture change is the instrumentation that will reveal it.\n\n**Test results**\n\n- `python -m py_compile mppi.py` OK; `pyright` on CI scope: 0 errors;\n  `ruff check .` clean.\n- `make test` equivalent: **744 passed, 33 deselected** (~3:10).\n- MCP functional suite: 37 passed locally (env-specific, still fails on the\n  test.yml runner — investigation continues).\n\n**Files touched**\n`src/shinro/controllers/mppi.py`, `tests/test_mcp_server_functional.py`,\n`.github/workflows/update-labnotes-and-index.yml` (→ `update-labnotes.yml`).\n\n\n\n### 2026-08-05 21:30 UTC — update\n\n\n\n### 2026-08-05 21:44 UTC — update\n\n\n\n### 2026-08-05 22:00 UTC — update\n\n\n\n### 2026-08-05 22:30 UTC — update\n\n\n\n### 2026-08-05 23:00 UTC — update\n\n\n\n### 2026-08-05 23:30 UTC — update\n\n\n\n### 2026-08-06 00:07 UTC — MPPI brought to codebase standard\n\n**What changed**\n\n`src/shinro/controllers/mppi.py` was a raw numpy script: `MPPI_Controller`\nsnake-case class, no `ArrayBackend`, no registry decorator, no `from_config`,\nno docstrings, no tests, `reset()` a no-op `pass`. Brought it up to the\nstandard set by `LQR`/`MPC_LTI`/`SlidingModeController`:\n\n- **Renamed** `MPPI_Controller` → `MPPIController` (PascalCase, matches\n  `PIDController`/`SlidingModeController`), registered as `\"MPPI\"` via\n  `@register_controller`.\n- **`ArrayBackend` support**: constructor takes `backend: ArrayBackend | None\n  = None`; `compute()` bridges the state in with `bk.to_numpy` and the action\n  out with `bk.from_numpy`. The sampling/rollout/softmax loop stays numpy\n  (torch has no `np.random.normal` equivalent) — same precedent as `MPC_LTI`\n  bridging to OSQP and `LQR` bridging to scipy DARE.\n- **`seed` param** — `np.random.default_rng(seed)` for reproducible sampling\n  (the raw script used the global `np.random`).\n- **`reset()`** now zeros the nominal control sequence and clears\n  `_last_epsilon`/`_last_costs` bookkeeping.\n- **`from_config`** — builds the controller from a TOML dict; `dynamics_fn` /\n  `cost_fn` are callables that can't live in TOML, so they're left `None` and\n  injected after construction (`ctrl.dynamics_fn = ...`), per user decision.\n  `compute()` raises a clear `RuntimeError` if they're unset.\n- **Guarded the K=1 shift** — `self.u[-1] = self.u[-2]` was an `IndexError`\n  for a horizon of one; only shift when `K > 1`.\n- **Full Sphinx docstrings** — module, class, methods, `Args:`/`Returns:`/\n  `Config fields:`/`Raises:` blocks matching codebase convention.\n- **Exported** `MPPIController` from `controllers/__init__.py` (+ docstring\n  list + `__all__`) and added `configs/controllers/mppi.t\n\n[truncated by preseed]",
      "source": "lab-notes/daily/2026-08-05.md"
    },
    {
      "id": "labnote_7d8a1d41016eaed48cabd309",
      "content": "# Lab note 2026-08-06.md\nSource: lab-notes/daily/2026-08-06.md\n\n# Lab Notes — 2026-08-06\n\n### 2026-08-07 02:22 UTC — port-numpy-component skill + AGENTS.md workflow consolidation\n\n**What changed**\n\n- **`.opencode/skills/port-numpy-component/SKILL.md` (new)** — promoted the\n  \"Porting Numpy Scripts to the Framework\" workflow out of AGENTS.md and into a\n  first-class, repo-local OpenCode skill that loads on-demand via the `skill`\n  tool. Kept the same 11-step sequence (understand → baseline test → backend-\n  agnostic swap → register → config → export → tests → lint → full suite →\n  docstrings → lab note) and enriched it with repo-specific anchors so an agent\n  doesn't re-derive them: ABCs in `src/shinro/components.py`, the\n  `@register_*` decorators in `factories/registry.py`, the backend abstraction\n  in `utils/array_backend.py`, per-subpackage `__init__.py` export points, the\n  existing `tests/test_*.py` templates, exact lint/test commands (`ruff check\n  .`, `pyright`, `make test`), the Sphinx docstring convention (`:math:`,\n  `Args:`/`Returns:`/`Config fields:`), and the mandatory\n  `lab-notes/daily/<date>.md` requirement. Frontmatter includes\n  `triggers`/`role`/`scope` metadata and `license: MIT`,\n  `compatibility: opencode` so it is discoverable and valid under the opencode\n  skills spec.\n- **`AGENTS.md`** — replaced the full 15-line \"Porting Numpy Scripts to the\n  Framework\" section with a one-line pointer to the skill. No longer\n  duplicated; the skill is now the single source of truth for the porting\n  workflow.\n\n**Key decisions**\n\n- **Project-local over global**: the skill lives in `.opencode/skills/` so it\n  is committed with the repo and shared with any contributor/agent working in\n  this checkout, rather than polluting `~/.config/opencode/skills/`.\n- **Config step corrected to match reality**: the old AGENTS.md workflow said\n  \"create a TOML config in `configs/<component_type>/`\". The packaged `configs/`\n  tree does exist under `src/shinro/configs/`, so the skill references that\n  location — no new top-level directory invented.\n- Kept the general 5-step dev loop in AGENTS.md (`## Workflow`) intact; only\n  the porting-specific workflow moved to the skill.\n\n**Test results**\n\n- No code touched — no tests run. `git show --stat` confirms the commit\n  (`29f0b6f`, branch `feat/controller-mppi`) contains only the new skill file,\n  the AGENTS.md trim, and this lab note.\n\n**Files touched**\n\n`.opencode/skills/port-numpy-component/SKILL.md` (new), `AGENTS.md`,\n`lab-notes/daily/2026-08-06.md`.\n\n\n\n### 2026-08-07 02:26 UTC — update",
      "source": "lab-notes/daily/2026-08-06.md"
    },
    {
      "id": "labnote_3797d153870e9b5061cfdde4",
      "content": "# Lab note 2026-08-10.md\nSource: lab-notes/daily/2026-08-10.md\n\n# Lab Notes — 2026-08-10\n\n### 2026-08-10 15:30 UTC — initial\n\n\n\n### 2026-08-10 16:00 UTC — update\n\n### 2026-08-10 17:00 UTC — configurable linearization point for CartPole & InvertedPendulum\n\n**What:** `get_model()` on `CartPole` and `InvertedPendulum` now linearizes about a\nuser-specified operating point `(x0, u0)` instead of being hard-coded to the\nupright equilibrium.\n\n**Why:** The closed-form Jacobians previously returned were only valid around\nthe origin (upright). Controllers/estimators want linear models about arbitrary\noperating points (e.g., pendulum near-horizontal, cartpole off-upright).\n\n**How:**\n- `get_model(self, x0=None, u0=None, eps=1e-6)` delegates to the existing\n  `shinro.utils.linearization.linearize()` helper (central finite differences)\n  on the plant's `self.dynamics`. Omitting `x0`/`u0` defaults to the upright\n  equilibrium with zero control, preserving backward compatibility — the\n  no-arg return value matches the previous closed-form matrices.\n- `linearize()` passes numpy arrays to `f` regardless of backend, so each plant\n  got a private `_dynamics_np(x, u)` wrapper that converts numpy → backend via\n  `self.bk.from_numpy`, runs `dynamics`, and converts back with\n  `self.bk.to_numpy`. This keeps NumpyBackend and TorchBackend both working.\n- Class docstrings updated: the linearization point is now configurable; the\n  analytic Jacobian block still documents the upright default.\n\n**Design decisions:**\n- Backward-compatible default (no-arg `get_model()` unchanged) rather than a\n  breaking required-arg API — all existing callers (`batched_adapter`,\n  `scenario_factory`, `components`, `scenario_runner`) keep working.\n- Always route through `linearize()` (even at upright) for a single code path,\n  per user preference, accepting finite-difference error (~1e-8) over the\n  previously exact closed form. Docstrings note the upright Jacobians remain\n  the documented default.\n- Applied to both plants for consistency (InvertedPendulum had the same\n  hard-coded pattern).\n\n**Tests:** 6 new tests in `tests/test_plants.py` (3 per plant):\n- upright model matches the analytic closed-form Jacobians (atol 1e-6)\n- no-arg `get_model()` ≡ explicit `get_model(zeros, zeros)` (atol 1e-12)\n- non-upright point produces an A distinct from the upright A\n\n**Verify:** full suite `pytest tests/` → 808 passed, 5 skipped; `ruff check`\nclean on edited files. `ruff format --check` flags pre-existing style in these\nfiles (single quotes, multiline arrays) unrelated to this change — left\nuntouched to keep the diff minimal. Note: `plants/double_pendulum.py` had\npre-existing uncommitted changes in the working tree, not part of this work.\n\n\n### 2026-08-10 16:30 UTC — update\n\n\n\n### 2026-08-10 17:00 UTC — update\n\n\n\n### 2026-08-10 19:30 UTC — update\n\n\n\n### 2026-08-10 20:00 UTC — update\n\n\n\n### 2026-08-10 20:30 UTC — update\n\n\n### 2026-08-10 21:00 UTC — extract numpy bridge + linearize_plant helpers, add Plant.input_dim\n\n**What:** Promoted the duplicated `_dynamics_np` wrapper into reusable utils and\nmade `linearize_plant`'s default control dimension correct for multi-input\nplants.\n\n**Why:** The `_dynamics_np` bridge (numpy ↔ backend conversion so `linearize()`\ncan call a backend-native `dynamics`) was copy-pasted into CartPole and\nInvertedPendulum. Every future nonlinear plant would need the same boilerplate.\nSeparately, a hardcoded `zeros(1)` default for `u0` is wrong for any plant with\nmulti-dim control (DoublePendulum=2, future Quadrotor=4, ArmRobot twist=6) and\nwould silently produce a wrong-shaped `(n_x, 1)` B.\n\n**How:**\n- `src/shinro/utils/linearization.py`:\n  - `as_numpy_f(f, backend)` — wraps any backend-bound `f(x, u)` into a\n    numpy-in/numpy-out callable via `backend.from_numpy`/`to_numpy`.\n  - `linearize_plant(plant, x0=None, u0=None, eps=1e-6)` — resolves `x0`\n    default from `len(plant.get_state())`, `u0` default from\n    `plant.input_dim`, bridges `plant.dynamics` through `as_numpy_f`, and\n    delegates to `linearize`.\n- `src/shinro/components.py`: added optional class attribute\n  `Plant.input_dim: int | None = None` (not abstract — no subclass breaks).\n  `linearize_plant` raises `ValueError` if `input_dim` is unset and `u0` is\n  not passed, so a multi-input plant can never silently get a wrong-dim `u0`.\n- `src/shinro/plants/cartpole.py` / `inverted_pendulum.py`: removed the\n  duplicate `_dynamics_np` method and the now-unused `import numpy as np`;\n  `get_model` is a one-liner `return linearize_plant(self, x0, u0, eps=eps)`;\n  `__init__` sets `self.input_dim = 1`.\n\n**Design decisions:**\n- `input_dim` is an optional attribute, not a new abstractmethod — plants that\n  don't use `linearize_plant` (ArmRobot, HolonomicMobileRobot, Quadrotor) are\n  untouched. They opt in with one line when they need it.\n- Safe-error over silent-wrong: `ValueError` when the control dim can't be\n  inferred, rather than defaulting to `zeros(1)`.\n- `scenario_factory.py` still derives `n_u` from `B.shape[1]`; reading\n  `plant.input_dim` there is a possible future cleanup, out of scope.\n\n**Tests:** 10 new tests in `tests/test_linearization.py`:\n- `TestAsNumpyF`: numpy-backend passthrough; torch-backend round-trip.\n- `TestLinearizePlant`: cartpole upright matches analytic Jacobians (atol\n  1e-6); defaults ≡ explicit zeros (atol 1e-12); raises `ValueError` when\n  `input_dim` unset and `u0` not passed; `u0` defaults to `zeros(input_dim)`\n  with B shape `(state_dim, input_dim)` for a 3-dim stub plant.\n\n**Verify:** full suite `pytest tests/` → 818 passed, 5 skipped (10 new);\n`ruff check` clean on all edited files.\n\n**Note:** `double_pendulum.py` is being worked through separately by the user\n(no changes made here). It will adopt `input_dim = 2` and, once it gains a\nbackend, `linearize_plant`.\n\n### 2026-08-10 21:00 UTC — update\n\n\n\n### 2026-08-10 21:30 UTC — update\n\n\n\n### 2026-08-10 22:00 UTC — first-class port of DoublePendulum plant\n\n**What:** Ported `DoublePendulum` into a first-class framework plant following\nthe `port-numpy-component` skill: backend-agnostic, TOML-configurable,\nregistered + exported, MuJoCo engine mode, MJCF detector, full test coverage,\ndocstrings. Also created `models/double_pendulum.xml`.\n\n**Why:** The previous file was a non-instantiable stub — it missed the abstract\n`physics_engine` method, had no `self.bk`, and contained three runtime bugs\n(out-of-bounds gravity indexing, a `np.array(x, y)` TypeError, and a `step()`\nthat overwrote the state instead of integrating).\n\n**How:**\n- `src/shinro/plants/double_pendulum.py` (rewrite mirroring InvertedPendulum):\n  - `__init__(mass_top, mass_bottom, length_top, length_bottom, dt, g,\n    state_bounds, backend)` sets `self.bk`, `input_dim = 2`, and validates\n    positive masses/lengths/dt (`ValueError`).\n  - All array ops via `self.bk` (`zeros`, `cos`, `sin`, `solve`, `stack`,\n    `copy`, `clip`, `array`) — runs under numpy and torch.\n  - Fixed `_make_gravity_vector` indexing (`G[0,0]`/`G[1,0]`, G is 1-D now),\n    fixed `np.array([...])` typo, and rewrote `step` as semi-implicit Euler\n    (`omega_new = omega + thetaddot*dt; theta_new = theta + omega_new*dt`).\n  - `get_model` → `linearize_plant(self, x0, u0, eps)` one-liner (reuses the\n    util extracted earlier today).\n  - `physics_engine` attach/detach + engine-branch `get_state` (reads\n    `hinge_1`/`hinge_2` qpos/qvel) and `step` (writes `torque_1`/`torque_2`).\n  - `from_config` parses all fields incl. `state_bounds` and optional `engine`\n    (fixed the `confif` typo).\n  - `@register_plant_detector(\"DoublePendulum\")` matches exactly 2 hinge\n    joints + 2 motor actuators (distinguishes from CartPole 1 slide + 1 hinge\n    and InvertedPendulum 1 hinge + 1 motor).\n- `models/double_pendulum.xml` (new MJCF): 2 hinge joints + 2 motors, radian\n  compiler, gravity `0 0 -9.81`, masses/lengths matching Python defaults.\n- `src/shinro/plants/__init__.py`: exported `DoublePendulum`.\n\n**Key design decisions:**\n- **Full MuJoCo engine mode** (per user) rather than a stub — required a new\n  MJCF model since none existed.\n- **Analytic-Jacobian math verification**: at rest (θ=0, ω=0) the manipulator\n  equation gives θ̈ = M(0)⁻¹(τ − Cω − G) with Cω=0, G(0)=0, so the test\n  computes expected B (rows 2–3 = M(0)⁻¹) and A (bottom-left 2×2 =\n  −M(0)⁻¹·∂G/∂θ|₀) from closed-form matrices and asserts `get_model()` matches\n  at atol 1e-6. This pins any sign/structure bug in M/C/G.\n- **Skipped `robot_config.toml`** — deliberate deviation from skill step 5:\n  that file models the real LeKiwi robot and a double pendulum isn't part of\n  it. `from_config` is exercised directly via tests.\n\n**Tests:** `TestDoublePendulum` in `tests/test_plants.py` (29 params over\nnumpy+torch): step shape / at-rest-stays / semi-implicit-Euler-accumulates,\ndynamics shape / gravity-balancing (τ cancels G → θ̈≈0), get_model\nshape / at-rest-matches-analytic / defaults≡explicit / nonzero-point,\nstate_bounds, from_config, invalid-config raises, detector matches/rejects,\nmock-engine attach+read and step-with-actuators.\n\n**Verify:** `pytest tests/` → 847 passed, 5 skipped (29 new); `make lint`\n(ruff + pyright) clean. Smoke check: `get_model()` → A(4,4), B(4,2); detector\nTrue for double_pendulum.xml, False for cartpole.xml/pendulum.xml.\n\n### 2026-08-10 23:00 UTC — review of DoublePendulum port: Coriolis bug + convention fixes\n\n**What:** Post-port review of `src/shinro/plants/double_pendulum.py` caught a\nlatent math bug inherited from the original user code, plus convention and\nhygiene issues. Fixed all four.\n\n**Findings & fixes:**\n1. **Coriolis matrix bug** (the important one). The original set\n   `C[0,0] = m2 l1 l2 sin(Δθ) ω2` and `C[0,1] = C[0,0]`, which makes\n   `C @ ω` row 0 = `m2 l1 l2 sin(Δθ) ω2 (ω1 + ω2)` — a spurious `ω1·ω2`\n   cross-term not present in the Euler-Lagrange velocity terms. The correct\n   E-L velocity-squared terms are `+m2 l1 l2 sin(Δθ) ω2²` (eq 1) and\n   `-m2 l1 l2 sin(Δθ) ω1²` (eq 2), which require `C[0,0]=0`,\n   `C[0,1]=m2 l1 l2 sin(Δθ) ω2`, `C[1,0]=-m2 l1 l2 sin(Δθ) ω1`, `C[1,1]=0`.\n   Fix: leave `C[0,0]` at 0 (from `zeros`), set `C[0,1]` directly. None of the\n   original tests caught this because they all used `ω=0`, where `C ω = 0`.\n2. **Docstring convention.** Class said \"angle from upright\" but the dynamics\n   (`+sin(θ)` gravity, stable equilibrium at θ=0) is the **downward/hanging**\n   convention. Fixed to \"angle from the downward vertical; rest equilibrium\n   θ=0 is stable (hanging down).\"\n3. **MJCF orientation.** `models/double_pendulum.xml` had links pointing\n   **up** (+z), making MuJoCo qpos=0 the inverted (unstable) pose —\n   inconsistent with the analytical model's hanging-down rest. Flipped all\n   geoms/body positions to -z so qpos=0 = hanging down = analytical rest.\n4. **Torch UserWarning.** `dynamics` did `self.bk.array(tau)` on an\n   already-backend tensor, triggering \"To copy construct from a tensor\"\n   warnings (10 per suite run). Removed the redundant re-wrap; `tau` is\n   already a backend array when `control` has `__len__`.\n\n**New test:** `test_dynamics_coriolis_matches_closed_form` in\n`TestDoublePendulum` — sets `g=0, τ=0` so only the Coriolis term contributes,\nuses nonzero `ω = [1.5, -0.7]`, and asserts `θ̈ = -M⁻¹ C ω` against the\nclosed-form `[m2 l1 l2 sin(Δθ) ω2², -m2 l1 l2 sin(Δθ) ω1²]` (atol 1e-9).\nThis would have failed against the old C matrix and pins the fix.\n\n**Verify:** `pytest tests/` → 849 passed, 5 skipped (1 new test, ×2 for\nnumpy/torch = 2); `make lint` clean. The 10 torch UserWarnings are gone.\n\n**Note:** The Coriolis bug was inherited from the user's original draft, not\nintroduced by the port. The port faithfully preserved it; this review caught\nit by deriving the E-L equations by hand. Lesson: tests at `ω=0` can't\nvalidate the velocity-dependent terms — a moving-state test is essential for\nany plant with a Coriolis/centrifugal term.\n\n### 2026-08-10 21:52 UTC — update\n\n\n\n### 2026-08-10 22:07 UTC — update",
      "source": "lab-notes/daily/2026-08-10.md"
    }
  ]
}