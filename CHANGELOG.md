# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Version numbers are derived from git tags via `setuptools-scm` — a release is
simply a `vX.Y.Z` tag pushed to `origin` (see `make release-patch`, `make
release-minor`, `make release-major` and the README "Releases" section).

This file is auto-generated by [`git cliff`](https://git-cliff.org) from
[Conventional Commits](https://www.conventionalcommits.org/). Do not edit it
by hand — adjust `cliff.toml` instead.

## [0.2.2] - 2026-09-10

### Added

- Add discrete-time model generation with Euler discretization and derive_model fallback support
- Add plant-derived model injection and dict config support to build and scenario generation
- Add oracle_tol compile option and new MPC controller configuration files
- Add full numpy broadcasting support, fix transpose and slice handling, and improve scalar clip bound processing
- Add controller output shape validation and overhaul plant scan test suite
- Add vec boolean field to Node struct and update code generation accordingly

### Documentation

- Record get_model discrete standardization in lab notes
- Document oracle_tol key in scenario template

### Fixed

- Increase MPC horizon to 100 and tighten control bounds for long-horizon stability

### Testing

- Remove duplicate n_vars mismatch test that fails without zig on PATH
- Add exhaustive op/shape matrix suite and enforce 2‑D tensor limit in lower_zig

## [0.2.1] - 2026-09-09

### Added

- Skip host oracle when building for non-native cross‑compile targets
- Skip host oracle on non-native targets and add informative note

## [0.2.0] - 2026-09-08

### Added

- Add shinro-testing skill and update AGENTS, components, and testing documentation
- Add plant-only scenario mode with derived dynamics, new configs and docs; fix: correct D matrix dimensions in KalmanFilter and LuenbergerObserver
- Add generic scenario config template
- Add compile target for end-to-end scenario building and update docs and README
- Add trace_component CLI to verify component tracing
- Add post-compile deployment record with master hash and hermetic TOML config hashing
- Add graph-conditional OSQP linking based on has_solve_qp flag and update docs/tests accordingly
- Strip DWARF in non-Debug builds and add stripped flag to manifest
- Enable stripping in release builds and include stripped flag in manifest
- Add ordered node list with dual op names and offsets to the build manifest
- Add deterministic build manifest audit trail with report and timestamped archive
- Add deterministic build manifest generation with solver metadata provenance
- Add -Dgraph and -Dsolver_dir build options with comptime n_vars validation
- Support controller recurrent state, fix frozen PID bugs, and adjust buffer length to 318
- Add ne operation support and expand graph data buffers, offsets, and clipping thresholds
- Document role-based controller wiring and add gen_mpc script for KF+MPC_LTI composition
- Add recurrent state_P handling for Kalman filter and update graph data offsets and docs
- Add static codegen solve_qp op and drop OSQP cimport path
- Integrate OSQP codegen static solver test and build support
- Integrate vendored OSQP library with Zig binding, add tests and benchmark
- Add OSQP third‑party makefile, CMakeLists and header files for embedded build
- Lower stack to Zig, completing the op enum
- Lower the 7 host-only ops and sin/cos to Zig
- Implement tanh and add argmax and onehot helper functions
- Implement matrix_power function with exponentiation support
- Add ReLU, elementwise exponential, matrix_power functions and return statement to cos_vec
- Add sin_vec function and std import for vectorized trig operations
- Migrate Zig build to runtime/build.zig, add build script, unit tests, and update .gitignore
- Add matmul, matvec, vecmat functions and improve linalg docs; relocate gen_base script and adjust Makefile and .gitignore
- Add Zig lowering pipeline with graph generation, build targets, and runtime VM implementation
- Add ONNX RL adapter controller with backend-agnostic support, tests, config and optional pyproject extra
- Add demo_codegen.py script demonstrating tracing, composition, and verification of estimators and controllers for base_tracking and cartpole scenarios
- Add compose module to stitch estimator and controller graphs into a closed-loop step graph
- Introduce shinro.codegen module with tracing backend, contract inference, and interpreter

### Build

- Add Zig build scaffolding with build.zig, build.zig.zon, unit tests, and Makefile updates

### Changed

- Update references to runtime directory to src/shinro/runtime across docs, Makefile, and markdown files
- Remove strip flag from release builds and add build size measurements to daily notes
- Split EMOSQP test into handwritten code and generated data module
- Replace vendored OSQP with generated static EMOSQP solver and update build, test configs and markers
- Add output array placeholder to cos_vec

### Documentation

- Trim AGENTS.md to high-signal instructions; fix stale CI workflow reference
- Add 2026-08-31 lab note for ticket workflow session
- Fill in 2026-08-27 lab note and add ticket-check step to workflow
- Switch MCP command to shinro-mcp and expand component, codegen, and README documentation
- Add plain-language guide and system architecture documentation
- Add codegen pipeline documentation

### Fixed

- Add repo root to pytest pythonpath so scripts.gen_base imports in CI
- Correct matmul usage in matrix_power and add missing return
- Implement cos_vec loop to compute cosine values and add ReLU comment
- Make MPPI tracking test robust across Python versions

### Style

- Expand inline loops to multi-line blocks for readability

### Testing

- Add 65 new codegen unit tests covering tracing, backend, ops, interpreter, and related modules

## [0.1.1] - 2026-08-18

### Added

- Add Double Pendulum plant, MPPI integration tests, and batched adapter
- Add configurable initialization arguments to MPPI_Controller
- Add MPPI controller implementation

### Fixed

- Make release targets POSIX-compatible ($((...)) on /bin/sh)

## [0.1.0] - 2026-08-05

### Added

- Add inverted pendulum, cartpole, quadrotor plants and XML config generator with documentation updates
- Add InvertedPendulum and CartPole plants, configs, XML models, and plant detector registry
- Add default Plant.dynamics method with documentation and extensive linearization tests
- Added mcp server for the shinro python modules
- Add configurable array backend (numpy/torch) with resolver and backend-aware operations across controllers, estimators, and trajectories
- Add PID output limits, MPC constraint tools, and separate controllability/observability analysis utilities
- Add system analysis and balanced truncation tools with tests and update CI dependencies
- Add trajectory creation, generation, and query tools to MCP server
- Add estimator creation, estimation, reset, and listing tools to MCP server
- Add controller and estimator config files, support custom MPC constraint matrix with default unconstrained bounds, and improve create_controller validation and related tests
- Add MCP server module with controller tools and comprehensive tests
- From_config accepts full matrices (nested lists) and custom A/B dynamics; add parse_matrix helper; add full-matrix tests
- Add axis/dim params to norm, sum, cross in ArrayBackend; add batched tensor tests for linalg methods
- Add finite‑horizon Gramian computation, spectral diagnostics, and balanced truncation utilities
- Add sparse matrix support, default zero B/C/D handling, rename cache variable, and implement discrete Gramian methods
- Add controllability and observability gramian methods with Hurwitz validation
- Implement observability matrix calculation and rank check
- Implement observability function in controllability_checker.py
- Return controllability flag and rank, rename matrix variable, add SVD step and observability stub
- Add controllability checker utility
- Add LeRobot diffusion adapter and configuration support
- Support start/end velocity and acceleration in cubic and quintic polynomial trajectories
- Support start/end velocity and acceleration in cubic and quintic polynomial config adapters
- Add simple demo script and extend factories with plant registry and registry‑based trajectory creation
- Add Luenberger observer config, new trajectory YAML files, register controllers with factory decorators and from_config methods, and include demo package with arm trajectory example
- Add controller, estimator, and trajectory factories with YAML config support
- Add generate_robot_config utility to create robot_config.yaml from MuJoCo XML models
- Add PhysicsEngine abstract base class with simulation interface methods
- Implement lekiwi robot base kinematics, arm control, and core control architecture with MPC/LQR/PID
- Add trajectory generation with cubic/quintic polynomial support and modular package structure
- Implement quintic polynomial trajectory generation with boundary conditions
- Add quintic polynomial trajectory generator and clip time bounds in cubic
- Extend cubic polynomial trajectory with velocity constraints
- Extend trajectory generator with cubic polynomial implementation and position_at method
- Implement cubic spline trajectory generator
- Refactor physics backend for robust ee position control via ik
- Add mujoco-based inverse kinematics with damped least-squares solver
- Implement arm extension demo with EE-space P-controller and cubic trajectory
- Add mpc controller option and trajectory presets
- Add 3D waypoint visualization and state estimation for base/arm tracking
- Add mujoco physics integration for armrobot state tracking and control
- Enhance armrobot with precise mujoco jacobian control and ee state tracking
- Implement pick-and-place demo with LQR base control and arm trajectory planning
- Add robot box-picking demo with MuJoCo physics integration
- Add lekiwi robot simulation demo with gif capture and physics improvements
- Add MuJoCo physics backend for robot simulation
- Add abstract trajectory generator class
- Add kalman filter and luenberger observer state estimators
- Implement complementary filter and complete kalman filter logic
- Enhance KalmanFilter with full state-space model support
- Add kalman filter and enhance luenberger observer with documentation and reset functionality
- Add optional initial state estimate to LuenbergerObserver
- Implement Luenberger observer with observer gain and default C/D matrices
- Add Luenberger observer implementation
- Enhance armrobot step with inverse kinematics and bounded step control
- Implement arm robot state update and inverse kinematics with pose target support
- Implement inverse kinematics for arm robot
- Enhance forward_kinematics to return end-effector pose and joint axes
- Add rotation axes parameter and implement forward kinematics
- Add joint_index parameter to homogenous_transform for offset calculation
- Extend armrobot with joint limits, offsets, and basic kinematics
- Implement arm robot dynamics with state tracking and getter
- Add arm robot plant component
- Introduce abstract state estimator interface
- Implement discrete-time linear quadratic regulator controller
- Implement discrete-time LQR controller class
- Introduce abstract controller and plant interfaces with holonomic robot implementation
- Add PID controller and MPC state matrices initialization
- Add holonomic mobile robot and MPC_LTI controller implementations
- Add osqp solver for MPC_LTI optimization problem
- Add terminal cost and constraint handling to LTI controller
- Implement holonomic robot kinematics and MPC controller
- Add time step parameter and basic kinematic state management to HolonomicMobileRobot
- Add holonomic mobile robot class for lekiwi kinematics
- Implement inverse kinematics matrix for lekiwi robot
- Add holonomic robot base kinematics implementation

### CI

- Add GitHub Actions test workflow; fix: convert MPC_LTI.F to numpy before transpose; refactor: use backend arrays in ArmRobot; test: add comprehensive ArmRobot tests and broaden exception handling
- Add GitHub Actions workflow, pytest config, and extensive test suite

### Changed

- Restructure project for proper packaging, CI enhancements, and updated import paths
- Tidy imports, remove duplicates and modernize type hints across modules
- Encapsulate controllability and observability checks into LTISystemsAnalyzer class
- Add backend-agnostic array operations across all components
- Add backend-agnostic array operations across all components
- Make framework config-driven with TOML and registry pattern
- Streamline capture_demo and capture_gif by using RobotSim, remove unused code/comments and simplify simulation setup
- Simplify demo to base-only control without arm/gripper logic
- Replace armrobot with iterative mujoco-based ik solver
- Improve arm jacobian handling, restructure mujoco scene hierarchy, and fix MPC constraint tiling
- Update _homogenous_transform to support multi-joint transformations with validation
- Optimize kinematics caching and rename mpc solve method to compute
- Rename MPC class to MPC_LTI and restructure dynamic matrix calculations

### Documentation

- Add changelog, release workflow, and release make targets
- Add extensive docstrings for CartPole and InvertedPendulum plants
- Expand project overview and component list in AGENTS.md
- Add entry for Google-style docstrings update
- Add Google-style docstrings to all components
- Update daily lab note for 2026-07-05 with lekiwi robot progress
- Add daily lab note for 2026-07-05 with lekiwi robot progress
- Add daily lab note for 2026-07-05 with lekiwi robot progress summary
- Add project documentation for codebase indexing and architecture overview
- Enhance package documentation with module descriptions and class docstrings
- Update lekiwi demo animations
- Update lekiwi arm demo animation
- Update lekiwi demo animation
- Enhance controller/plant/estimator/trajectory base classes with detailed descriptions and usage examples
- Added documentation and README
- Add comprehensive docstrings to abstract base classes and controllers

### Fixed

- Return native int and float types for controllability and observability metrics
- Added max horizon limit for MPC
- Pyright-clean — add asserts for _engine null safety, replace torch.block with cat, add type:ignore for lerobot import
- Drop py3.10/3.11 from CI matrix — pinned deps require >=3.12
- Add Any type arg to bare Optional annotations for pyright
- Install requirements.txt (numpy, scipy, osqp) in CI workflow
- Use bk.hstack for backend-safe state construction in ArmRobot; fix: use FK-known target for IK reachability test
- Remove empty lines and trailing whitespace in holonomic robot matrix calculation
- Remove trailing newline in lqr.py
- Replace @ operator for matrix multiplication and fix osqp solver constraints

### Style

- Add mcp <2 version bound and reformat factory and server modules for readability

### Testing

- Allow inv and svd to raise LinAlgError or propagate NaN in adversarial tests

[0.2.2]: https://github.com/Shinro-xyz/shinro-python-modules/compare/v0.2.1..v0.2.2
[0.2.1]: https://github.com/Shinro-xyz/shinro-python-modules/compare/v0.2.0..v0.2.1
[0.2.0]: https://github.com/Shinro-xyz/shinro-python-modules/compare/v0.1.1..v0.2.0
[0.1.1]: https://github.com/Shinro-xyz/shinro-python-modules/compare/v0.1.0..v0.1.1
[0.1.0]: https://github.com/Shinro-xyz/shinro-python-modules/tree/v0.1.0

<!-- generated by git-cliff -->
